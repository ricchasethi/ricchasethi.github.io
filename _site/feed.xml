<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-05-27T13:14:42+10:00</updated><id>/feed.xml</id><title type="html">Research to Production</title><subtitle>Research to production mindset</subtitle><entry><title type="html">BioRAG: Part 4</title><link href="/2026/05/27/BioRAG-part4-llm-synthesis-server-mcp.html" rel="alternate" type="text/html" title="BioRAG: Part 4" /><published>2026-05-27T00:00:00+10:00</published><updated>2026-05-27T00:00:00+10:00</updated><id>/2026/05/27/BioRAG-part4-llm-synthesis-server-mcp</id><content type="html" xml:base="/2026/05/27/BioRAG-part4-llm-synthesis-server-mcp.html"><![CDATA[<h1 id="building-biorag-a-decision-support-rag-system-for-biomedical-literature">Building BioRAG: A Decision-Support RAG System for Biomedical Literature</h1>

<p><strong>Part 4 of 5 — LLM Answer Synthesis, Prompt Design, and Serving the Engine</strong></p>

<hr />

<h2 id="where-we-left-off">Where We Left Off</h2>

<p>Post 3 ended with a fully assembled <code class="language-plaintext highlighter-rouge">DecisionOutput</code>.Confidence score derived from four explicit inputs, reasoning chain documenting every inference step, knowledge gaps surfaced as first-class fields. The rule-based <code class="language-plaintext highlighter-rouge">AnswerSynthesizer</code> concatenated excerpts, flagged contradictions with a warning marker, and found common terms across sources.</p>

<p>That is a valid baseline. It is auditable, dependency-free, and fast. But it does not write prose. For users who want natural language output, BioRAG provides <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> — a drop-in replacement that swaps only the prose generation step while leaving the rest of the pipeline identical.</p>

<p>This post covers three things: the design of <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> and the prompt choices that shaped it, the FastAPI server and the MCP server.</p>

<hr />

<h2 id="the-llm-synthesizer">The LLM Synthesizer</h2>

<p><code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> is a <strong>subclass</strong> of <code class="language-plaintext highlighter-rouge">AnswerSynthesizer</code>, not a parallel implementation. It overrides exactly one method: <code class="language-plaintext highlighter-rouge">synthesize()</code>. The reasoning chain construction, confidence formula, gap detection, and follow-up generation are all inherited without modification.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ClaudeSynthesizer</span><span class="p">(</span><span class="n">AnswerSynthesizer</span><span class="p">):</span>
    <span class="n">MODEL</span> <span class="o">=</span> <span class="s">"claude-sonnet-4-6"</span>
    <span class="n">MAX_TOKENS</span> <span class="o">=</span> <span class="mi">1500</span>
</code></pre></div></div>

<p>By the time a prompt reaches Claude, scoring, confidence computation, and gap detection have already run. Claude’s only job is to write 2–4 paragraphs from the numbered excerpts it receives. This separation prevents the LLM from filling evidence gaps with training knowledge. This can be a problem that is much harder to enforce when the model owns the full pipeline.</p>

<p>The <code class="language-plaintext highlighter-rouge">anthropic</code> client is lazy-initialized so the import is never triggered for users running only the rule-based engine, preserving the core’s zero-dependency guarantee.</p>

<hr />

<h2 id="the-system-prompt">The System Prompt</h2>

<p>The system prompt is a module-level constant in <code class="language-plaintext highlighter-rouge">llm_synthesizer.py</code> so it is easy to <code class="language-plaintext highlighter-rouge">grep</code>, version-control, and audit without reading the class.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">SYSTEM_PROMPT</span> <span class="o">=</span> <span class="s">"""</span><span class="se">\
</span><span class="s">You are a biomedical decision-support assistant integrated into a Retrieval-</span><span class="se">\
</span><span class="s">Augmented Generation (RAG) system called BioRAG.

## Your task
Answer the researcher's question using ONLY the numbered evidence excerpts </span><span class="se">\
</span><span class="s">provided in the user message.

## Strict grounding rules
1. Every factual claim in your answer MUST be traceable to at least one of </span><span class="se">\
</span><span class="s">the supplied excerpts.  Cite sources inline as [N] where N is the excerpt number.
2. Do NOT use knowledge from your training data.  If the excerpts do not </span><span class="se">\
</span><span class="s">contain sufficient information, say:
   "The indexed corpus does not contain sufficient evidence to answer this </span><span class="se">\
</span><span class="s">question.  Consider ingesting additional papers on this topic."
3. If excerpts contradict each other, acknowledge the contradiction explicitly
and cite both sides.
4. Do not speculate, extrapolate, or infer beyond what the text states.

## Output format
Write 2–4 concise paragraphs of plain prose (no markdown headers, no bullet </span><span class="se">\
</span><span class="s">lists).  Begin with a direct answer to the question, then elaborate with </span><span class="se">\
</span><span class="s">supporting detail and citations.  End with a brief sentence noting any </span><span class="se">\
</span><span class="s">important limitations visible in the evidence.

## Evidence quality legend
- DIRECT — explicit statistical results or clinical findings relevant to the query.
- INDIRECT — topically related but does not directly confirm the query.
- CONTRADICTORY — contains language suggesting refutation or failure.
"""</span>
</code></pre></div></div>

<h3 id="why-training-knowledge-is-forbidden">Why training knowledge is forbidden</h3>

<p>An LLM trained on biomedical literature knows a great deal about Alzheimer’s disease and pTau217. That knowledge is often correct. The problem is that it is not traceable. “pTau217 has an AUC of 0.94” means something different if it comes from a 2024 validation cohort of 800 patients than from a 2019 pilot of 40. The cohort characteristics and replication status are not knowable from a claim that originates in parametric memory.</p>

<h3 id="why-rule-4-exists">Why rule 4 exists</h3>

<p>Rule 4 — “Do not speculate, extrapolate, or infer beyond what the text states” — was added after a failure case in development.</p>

<p>The query was <em>“What is the diagnostic performance of plasma pTau217 for Alzheimer’s disease?”</em>. The first three paragraphs were correctly grounded: AUC values cited from [1] and [2], the contradictory replication finding from [3] acknowledged, community-based sample limitations noted. Then the model added:</p>

<blockquote>
  <p><em>“Given the trajectory of plasma biomarker development and declining cost of mass spectrometry platforms, pTau217-based testing is likely to become part of routine clinical practice within the next decade.”</em></p>
</blockquote>

<p>No excerpt said that. The sentence was plausible and a knowledgeable reader might have agreed with it. Which is exactly what makes it dangerous. It was not marked as speculative. Detection was straightforward: <code class="language-plaintext highlighter-rouge">engine.synthesizer.last_prompt["user"]</code> showed the full excerpt set and none contained a forward-looking clinical practice claim.</p>

<p>The fix was rule 4 plus a phrasing change in the output format instruction. The original read “elaborate with supporting detail”; the revision reads “elaborate with supporting detail <em>and citations</em>”, ensuring elaboration cannot license detail-addition without a citation anchor.</p>

<hr />

<h2 id="the-user-message">The User Message</h2>

<p><code class="language-plaintext highlighter-rouge">_build_user_message()</code> produces the exact string that appears in the <code class="language-plaintext highlighter-rouge">messages</code> array:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">e</span> <span class="ow">in</span> <span class="nb">enumerate</span><span class="p">(</span><span class="n">evidence</span><span class="p">,</span> <span class="mi">1</span><span class="p">):</span>
    <span class="n">support_label</span> <span class="o">=</span> <span class="n">e</span><span class="p">.</span><span class="n">support_type</span><span class="p">.</span><span class="n">upper</span><span class="p">()</span>
    <span class="n">excerpt</span> <span class="o">=</span> <span class="n">TextProcessor</span><span class="p">.</span><span class="n">truncate_at_sentence</span><span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">excerpt</span><span class="p">,</span> <span class="mi">500</span><span class="p">)</span>
    <span class="n">lines</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
        <span class="sa">f</span><span class="s">"</span><span class="se">\n</span><span class="s">[</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="s">] **</span><span class="si">{</span><span class="n">e</span><span class="p">.</span><span class="n">doc_title</span><span class="si">}</span><span class="s">** | Section: </span><span class="si">{</span><span class="n">e</span><span class="p">.</span><span class="n">section</span><span class="si">}</span><span class="s"> | "</span>
        <span class="sa">f</span><span class="s">"Relevance: </span><span class="si">{</span><span class="n">e</span><span class="p">.</span><span class="n">relevance_score</span><span class="si">:</span><span class="p">.</span><span class="mi">0</span><span class="o">%</span><span class="si">}</span><span class="s"> | Type: </span><span class="si">{</span><span class="n">support_label</span><span class="si">}</span><span class="se">\n</span><span class="s">"</span>
        <span class="sa">f</span><span class="s">"Excerpt: </span><span class="si">{</span><span class="n">excerpt</span><span class="si">}</span><span class="s">"</span>
    <span class="p">)</span>
</code></pre></div></div>

<p>Three things in the excerpt header carry signal.</p>

<p><strong>Support type labels.</strong> Each header includes <code class="language-plaintext highlighter-rouge">DIRECT</code>, <code class="language-plaintext highlighter-rouge">INDIRECT</code>, or <code class="language-plaintext highlighter-rouge">CONTRADICTORY</code>. The model already knows what these mean from the system prompt, so it knows before reading whether it is about to encounter a primary finding or a refutation — and can frame it accordingly in prose.</p>

<p><strong>Relevance percentages.</strong> The reranker’s score is passed through to the model. An excerpt at 64% should not be weighted the same as one at 100%. Providing the percentage means the model can structure its answer to lead with the strongest evidence.</p>

<p><strong>Gap list in the user message.</strong> Gaps from <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> are listed in a “Known knowledge gaps” section. This prevents the model from treating absence of evidence as evidence of absence. If the gap list says “quantitative data not found”, the model knows the corpus lacked the statistics, not that the statistics do not exist. It also makes the limitations sentence at the end of the answer specific rather than generic boilerplate.</p>

<p>The grounding instruction is repeated at the end of the user message (“Using ONLY the excerpts above…”) as a recency anchor just before generation.</p>

<hr />

<h2 id="prompt-caching-and-temperature">Prompt Caching and Temperature</h2>

<p>The system prompt is passed with <code class="language-plaintext highlighter-rouge">"cache_control": {"type": "ephemeral"}</code>. The cache TTL is 5 minutes. For a decision-support session where a clinician iterates through related queries, refining an initial question and following up on a gap — the system prompt tokens are processed once and cached for all subsequent queries in the session. At current pricing this reduces the effective per-query cost of the synthesis step by roughly 90% for those cached tokens.</p>

<p>Temperature is 0. Evidence synthesis in a clinical context has no use for sampling variation. The model should produce the same answer from the same evidence every time.</p>

<hr />

<h2 id="fallback-on-api-failure">Fallback on API Failure</h2>

<p>If the API call fails, <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> falls back to the parent’s rule-based <code class="language-plaintext highlighter-rouge">_build_answer()</code> and records it in the reasoning chain: <code class="language-plaintext highlighter-rouge">"LLM synthesis failed (...); used rule-based fallback."</code> The confidence score and gap list are unaffected. A decision-support system that becomes unusable when an external API is temporarily unavailable is not suitable for real use.</p>

<hr />

<h2 id="the-fastapi-server">The FastAPI Server</h2>

<p><code class="language-plaintext highlighter-rouge">server.py</code> exposes five endpoints:</p>

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Path</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">GET</code></td>
      <td><code class="language-plaintext highlighter-rouge">/health</code></td>
      <td>Corpus stats; confirms engine loaded</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">POST</code></td>
      <td><code class="language-plaintext highlighter-rouge">/query</code></td>
      <td>Full RAG pipeline; returns <code class="language-plaintext highlighter-rouge">QueryResponse</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">POST</code></td>
      <td><code class="language-plaintext highlighter-rouge">/ingest</code></td>
      <td>Fetch PubMed/PMC papers and index them</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">POST</code></td>
      <td><code class="language-plaintext highlighter-rouge">/documents</code></td>
      <td>Add a single document by text</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">GET</code></td>
      <td><code class="language-plaintext highlighter-rouge">/corpus</code></td>
      <td>Document/chunk/term count statistics</td>
    </tr>
  </tbody>
</table>

<p>The query response includes <code class="language-plaintext highlighter-rouge">latency_ms</code> in the body so clients can detect when retrieval slows as the corpus grows. This a signal to tune <code class="language-plaintext highlighter-rouge">retrieval_top_k</code> or chunk size.</p>

<p>The <code class="language-plaintext highlighter-rouge">/ingest</code> endpoint is synchronous. It blocks until NCBI returns all results, which is fine for <code class="language-plaintext highlighter-rouge">max_results</code> values of 10–20 but will time out for large requests. The right fix is background ingestion with a polling endpoint which is exactly what the MCP server implements.</p>

<p>Every request and response type is a Pydantic model. <code class="language-plaintext highlighter-rouge">QueryResponse</code> mirrors <code class="language-plaintext highlighter-rouge">DecisionOutput</code> with the addition of <code class="language-plaintext highlighter-rouge">latency_ms</code>. This is a serialization contract: it prevents internal dataclass fields like <code class="language-plaintext highlighter-rouge">frozenset</code> from leaking into the HTTP response.</p>

<hr />

<h2 id="the-mcp-server">The MCP Server</h2>

<p><code class="language-plaintext highlighter-rouge">mcp_server.py</code> exposes four tools callable directly inside a Claude Code session:</p>

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>What it does</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">query(question)</code></td>
      <td>Full RAG pipeline — answer, confidence, evidence, gaps, follow-ups</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ingest(pubmed_query, max_results)</code></td>
      <td>Starts a background ingestion job; returns <code class="language-plaintext highlighter-rouge">job_id</code> immediately</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ingest_status(job_id)</code></td>
      <td>Polls the job; includes <code class="language-plaintext highlighter-rouge">corpus_stats</code> when done</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">corpus_stats()</code></td>
      <td>Document/chunk/term counts</td>
    </tr>
  </tbody>
</table>

<h3 id="background-ingest">Background ingest</h3>

<p>MCP tool calls run inside a conversational context. A tool that blocks for 30 seconds on NCBI responses would time out the transport. The solution is a job registry: <code class="language-plaintext highlighter-rouge">ingest()</code> starts a daemon thread and returns a <code class="language-plaintext highlighter-rouge">job_id</code> immediately. <code class="language-plaintext highlighter-rouge">ingest_status()</code> polls it.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">@</span><span class="n">mcp</span><span class="p">.</span><span class="n">tool</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">ingest</span><span class="p">(</span><span class="n">pubmed_query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">max_results</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">10</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="n">job_id</span> <span class="o">=</span> <span class="n">uuid</span><span class="p">.</span><span class="n">uuid4</span><span class="p">().</span><span class="nb">hex</span><span class="p">[:</span><span class="mi">8</span><span class="p">]</span>
    <span class="n">_jobs</span><span class="p">[</span><span class="n">job_id</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="s">"status"</span><span class="p">:</span> <span class="s">"running"</span><span class="p">,</span> <span class="p">...}</span>
    <span class="n">threading</span><span class="p">.</span><span class="n">Thread</span><span class="p">(</span><span class="n">target</span><span class="o">=</span><span class="n">_ingest_job</span><span class="p">,</span> <span class="n">args</span><span class="o">=</span><span class="p">(</span><span class="n">job_id</span><span class="p">,</span> <span class="n">pubmed_query</span><span class="p">,</span> <span class="n">max_results</span><span class="p">),</span>
                     <span class="n">daemon</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="n">start</span><span class="p">()</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"job_id"</span><span class="p">:</span> <span class="n">job_id</span><span class="p">,</span> <span class="s">"status"</span><span class="p">:</span> <span class="s">"running"</span><span class="p">,</span> <span class="s">"message"</span><span class="p">:</span> <span class="s">"..."</span><span class="p">}</span>
</code></pre></div></div>

<p>The polling pattern in a Claude Code session looks like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use biorag to ingest 20 papers on CRISPR cancer therapy
→ { "job_id": "a3f9c1d2", "status": "running" }

Use biorag ingest_status for a3f9c1d2
→ { "status": "done", "indexed": 18, "corpus_stats": { ... } }
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">BioRAGEngine</code> carries an internal <code class="language-plaintext highlighter-rouge">threading.Lock</code> that serialises concurrent calls, so an ingest job running alongside a query is safe without additional locking at the server layer. A query during active ingestion sees the index in whatever state the lock was last released.</p>

<h3 id="registration">Registration</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install</span> <span class="s2">"mcp[cli]"</span>
claude mcp add biorag python /absolute/path/to/mcp_server.py
claude mcp list   <span class="c"># → biorag: python ... ✓ Connected</span>
</code></pre></div></div>

<p>The server starts as a child process over <code class="language-plaintext highlighter-rouge">stdio</code> transport. MCP tools are injected at session startup — restart Claude Code after first registration.</p>

<h3 id="using-the-tools">Using the tools</h3>

<p>Once registered, the tools are available in any Claude Code session by name. You address them in natural language and Claude Code calls them directly — no Python required.</p>

<p><strong>Query the indexed corpus:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use biorag to query: what plasma biomarkers predict Alzheimer's disease?
</code></pre></div></div>

<p>Claude Code calls <code class="language-plaintext highlighter-rouge">query()</code> and receives the full <code class="language-plaintext highlighter-rouge">DecisionOutput</code> — answer text, confidence label, evidence nodes with relevance scores, knowledge gaps, and follow-up questions. Each follow-up can be sent as the next query against the same live index.</p>

<p><strong>Ingest papers from PubMed:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use biorag to ingest 15 papers on plasma pTau217 Alzheimer's biomarkers
</code></pre></div></div>

<p>This returns a <code class="language-plaintext highlighter-rouge">job_id</code> immediately. Poll for completion:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use biorag ingest_status to check job a3f9c1d2
</code></pre></div></div>

<p>When status is <code class="language-plaintext highlighter-rouge">"done"</code>, the response includes updated corpus stats — document count, chunk count, full-text vs abstract breakdown — confirming the papers landed before you query.</p>

<p><strong>Check what is indexed:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use biorag corpus_stats
</code></pre></div></div>

<p>Useful before a query session to confirm the corpus covers the topic. If <code class="language-plaintext highlighter-rouge">full_text_count</code> is low relative to <code class="language-plaintext highlighter-rouge">doc_count</code>, most documents are abstract-only — a signal that retrieval precision may be limited by shallow text.</p>

<p><strong>A typical session:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. corpus_stats          → 8 docs, 312 chunks, 4 full-text
2. ingest 20 papers on "plasma biomarkers Alzheimer diagnosis"
3. ingest_status &lt;id&gt;    → done, 18 indexed, 312 → 1047 chunks
4. query: what AUC values have been reported for pTau217?
5. query: are there contradictory findings on plasma NfL?
6. query: which biomarkers have been validated in community cohorts?
</code></pre></div></div>

<p>Papers ingested in step 2 are available to all subsequent queries without re-ingestion. Each query returns an independent <code class="language-plaintext highlighter-rouge">DecisionOutput</code> with its own confidence score and gap list.</p>

<hr />

<h2 id="what-the-llm-does-and-does-not-own">What the LLM Does and Does Not Own</h2>

<p>A reasonable question: why not let the LLM run the whole pipeline? It knows biomedical text.</p>

<p>The structured pipeline does things the LLM cannot do reliably. BM25 scoring is exact arithmetic over a data structure: a token either matches or it does not. The confidence formula produces a checkable number: <code class="language-plaintext highlighter-rouge">0.3 + 0.4 + 0.14 − 0.05 = 0.81</code> is verifiable by a human reviewer; “High confidence” produced by an LLM is not. The gap detector fires on structural properties of the evidence set that do not require reading comprehension, and its false-positive rate is knowable from the pattern definitions.</p>

<p>What the LLM does well — and what the structured components do poorly — is <em>writing</em>. <code class="language-plaintext highlighter-rouge">_build_answer()</code> concatenates excerpts. <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> compares them, qualifies them, and acknowledges contradictions in context. That is the right division of labour: deterministic structure where auditability matters, natural language where readability matters.</p>

<hr />

<h2 id="coming-up-in-post-5">Coming Up in Post 5</h2>

<p>Post 5 covers the retrieval evaluation harness — the <code class="language-plaintext highlighter-rouge">evals/</code> directory that measures BM25 and reranker quality independently using MRR and NDCG@K. We will look at the 16-query ground truth set, how max-pooling aggregates chunk-level scores to document-level, and what the Δ column in the eval output actually tells you — when a negative NDCG delta is a reranker bug versus an expected trade-off from tightening <code class="language-plaintext highlighter-rouge">rerank_top_k</code>. We will also look at what retrieval evals cannot measure and what a full answer-quality evaluation would require.</p>

<hr />

<p><em>BioRAG is built in pure Python stdlib. The core engine — BM25 index, reranker, evidence classifier, synthesizer — has zero external dependencies. PubMed ingestion requires only <code class="language-plaintext highlighter-rouge">requests</code>. LLM answer synthesis requires only <code class="language-plaintext highlighter-rouge">anthropic</code>.</em></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Building BioRAG: A Decision-Support RAG System for Biomedical Literature]]></summary></entry><entry><title type="html">BioRAG: Part 3</title><link href="/2026/05/20/BioRAG-part3-knowledge-gaps-confidence-reasoning.html" rel="alternate" type="text/html" title="BioRAG: Part 3" /><published>2026-05-20T00:00:00+10:00</published><updated>2026-05-20T00:00:00+10:00</updated><id>/2026/05/20/BioRAG-part3-knowledge-gaps-confidence-reasoning</id><content type="html" xml:base="/2026/05/20/BioRAG-part3-knowledge-gaps-confidence-reasoning.html"><![CDATA[<h1 id="building-biorag-a-decision-support-rag-system-for-biomedical-literature">Building BioRAG: A Decision-Support RAG System for Biomedical Literature</h1>

<p><strong>Part 3 of 5 — Knowledge Gap Detection, Confidence Scoring, and Reasoning Chains</strong></p>

<hr />

<h2 id="where-we-left-off">Where We Left Off</h2>

<p>Post 2 ended with 5 structured <code class="language-plaintext highlighter-rouge">EvidenceNode</code> objects — promoted from 15 raw BM25 candidates by the Reranker and labelled <code class="language-plaintext highlighter-rouge">direct</code>, <code class="language-plaintext highlighter-rouge">indirect</code>, or <code class="language-plaintext highlighter-rouge">contradictory</code> by the EvidenceClassifier. The lung cancer paper is gone. Contradictory evidence is flagged explicitly. The pipeline knows <em>what</em> it found.</p>

<p>The next question is harder: <em>what didn’t it find, and how certain should it be about what it did find?</em></p>

<p>These two questions have to be answered before any prose is written. In a clinical decision-support context, the confidence estimate is the system communicating something auditable to a downstream human: <em>this answer rests on solid direct evidence from multiple corroborating sources</em> versus <em>this answer is assembled from partially relevant documents and you should verify it</em>.</p>

<p>This post covers the three components that answer those questions:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> — five structured checks for what the corpus is missing</li>
  <li><code class="language-plaintext highlighter-rouge">AnswerSynthesizer</code> — a four-step reasoning chain and a confidence formula with explicit inputs</li>
  <li><code class="language-plaintext highlighter-rouge">FollowUpGenerator</code> — intent-aware questions that move the user toward a more answerable query</li>
</ol>

<hr />

<h2 id="the-full-pipeline-after-post-2">The Full Pipeline After Post 2</h2>

<p>Before going deeper, here is what the pipeline has produced at the end of Post 2:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">evidence_nodes</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">EvidenceNode</span><span class="p">(</span>
        <span class="n">doc_title</span><span class="o">=</span><span class="s">"Plasma Aβ42/p-Tau217 ratio as a blood-based biomarker..."</span><span class="p">,</span>
        <span class="n">section</span><span class="o">=</span><span class="s">"Results"</span><span class="p">,</span>
        <span class="n">relevance_score</span><span class="o">=</span><span class="mf">1.000</span><span class="p">,</span>
        <span class="n">support_type</span><span class="o">=</span><span class="s">"direct"</span><span class="p">,</span>
        <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"pTau217"</span><span class="p">,</span> <span class="s">"AUC"</span><span class="p">,</span> <span class="s">"0.94"</span><span class="p">,</span> <span class="s">"95% CI"</span><span class="p">,</span> <span class="s">"Alzheimer"</span><span class="p">],</span>
    <span class="p">),</span>
    <span class="n">EvidenceNode</span><span class="p">(</span>
        <span class="n">doc_title</span><span class="o">=</span><span class="s">"Diagnostic Blood-Based Biomarkers for Alzheimer's Disease"</span><span class="p">,</span>
        <span class="n">section</span><span class="o">=</span><span class="s">"Results"</span><span class="p">,</span>
        <span class="n">relevance_score</span><span class="o">=</span><span class="mf">0.832</span><span class="p">,</span>
        <span class="n">support_type</span><span class="o">=</span><span class="s">"direct"</span><span class="p">,</span>
        <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"Amyloid"</span><span class="p">,</span> <span class="s">"NfL"</span><span class="p">,</span> <span class="s">"significant"</span><span class="p">,</span> <span class="s">"Alzheimer"</span><span class="p">],</span>
    <span class="p">),</span>
    <span class="n">EvidenceNode</span><span class="p">(</span>
        <span class="n">doc_title</span><span class="o">=</span><span class="s">"Longitudinal Changes in Plasma Biomarkers..."</span><span class="p">,</span>
        <span class="n">section</span><span class="o">=</span><span class="s">"Discussion"</span><span class="p">,</span>
        <span class="n">relevance_score</span><span class="o">=</span><span class="mf">0.641</span><span class="p">,</span>
        <span class="n">support_type</span><span class="o">=</span><span class="s">"contradictory"</span><span class="p">,</span>
        <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"plasma"</span><span class="p">,</span> <span class="s">"sensitivity"</span><span class="p">,</span> <span class="s">"failed"</span><span class="p">,</span> <span class="s">"clinical threshold"</span><span class="p">],</span>
    <span class="p">),</span>
    <span class="c1"># ...
</span><span class="p">]</span>
</code></pre></div></div>

<p>These nodes are the input to everything that follows. Two components next are: the <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> and <code class="language-plaintext highlighter-rouge">AnswerSynthesizer</code>. Gaps are detected first because they feed into the confidence formula.</p>

<hr />

<h2 id="stage-4-knowledge-gap-detection">Stage 4: Knowledge Gap Detection</h2>

<p>The <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> asks a structurally different question: <em>what does the returned evidence fail to cover that the query asked about?</em></p>

<p>This is the difference between retrieval quality and answer quality. A system can retrieve highly relevant documents and still be unable to answer the specific question. This could be due to the fact that the documents cover the right disease but the wrong endpoint, or the right biomarker but only in a different patient population, or the right intervention but without quantitative effect sizes.</p>

<p>BioRAG implements five gap checks.</p>

<h3 id="gap-1-entity-coverage">Gap 1: Entity Coverage</h3>

<p>Every entity extracted by the <code class="language-plaintext highlighter-rouge">QueryAnalyzer</code> should appear somewhere in the returned evidence. If a named entity from the query is absent from all five evidence excerpts, that is a coverage failure.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">covered_text</span> <span class="o">=</span> <span class="s">' '</span><span class="p">.</span><span class="n">join</span><span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">excerpt</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span><span class="p">).</span><span class="n">lower</span><span class="p">()</span>
<span class="k">for</span> <span class="n">entity</span> <span class="ow">in</span> <span class="n">query_analysis</span><span class="p">[</span><span class="s">"entities"</span><span class="p">]:</span>
    <span class="k">if</span> <span class="n">entity</span><span class="p">.</span><span class="n">lower</span><span class="p">()</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">covered_text</span><span class="p">:</span>
        <span class="n">gaps</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
            <span class="sa">f</span><span class="s">"No direct evidence found for '</span><span class="si">{</span><span class="n">entity</span><span class="si">}</span><span class="s">' in the indexed documents"</span>
        <span class="p">)</span>
</code></pre></div></div>

<p>This check is simple and catches the most straightforward failure mode: the corpus was queried for entity X, and entity X does not appear in any of the top-5 evidence nodes. This can happen even when BM25 retrieves documents that mention X, if those documents were demoted by the reranker (perhaps because their section weighting was weak) and replaced by documents that matched generic query tokens without containing the entity.</p>

<p><strong>What it catches:</strong> A query for “pTau217 and Alzheimer’s disease” where the corpus only contains Alzheimer’s papers that discuss amyloid-β exclusively — the entity “pTau217” is absent from all returned excerpts.</p>

<p><strong>What it misses:</strong> Near-synonym failure — a query using “neurofilament light” when the corpus uses “NfL” everywhere, and the abbreviation expansion map does not cover it.</p>

<h3 id="gap-2-quantitative-data">Gap 2: Quantitative Data</h3>

<p>For treatment, prognosis, and epidemiology queries, an answer without numerical evidence is not an answer. It is background knowledge only. The <code class="language-plaintext highlighter-rouge">QueryAnalyzer</code> flags queries with <code class="language-plaintext highlighter-rouge">needs_quantitative = True</code> for these intents. The gap detector checks whether at least one evidence node contains statistical notation.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">query_analysis</span><span class="p">[</span><span class="s">"needs_quantitative"</span><span class="p">]:</span>
    <span class="n">has_stats</span> <span class="o">=</span> <span class="nb">any</span><span class="p">(</span>
        <span class="n">re</span><span class="p">.</span><span class="n">search</span><span class="p">(</span><span class="sa">r</span><span class="s">'\d+\.?\d*%|\bp\s*[&lt;=&gt;]|odds ratio|hazard ratio'</span><span class="p">,</span> <span class="n">e</span><span class="p">.</span><span class="n">excerpt</span><span class="p">.</span><span class="n">lower</span><span class="p">())</span>
        <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span>
    <span class="p">)</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">has_stats</span><span class="p">:</span>
        <span class="n">gaps</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
            <span class="s">"Quantitative data (effect sizes, p-values, confidence intervals) "</span>
            <span class="s">"not found in retrieved evidence"</span>
        <span class="p">)</span>
</code></pre></div></div>

<p><strong>What this buys you:</strong> For a prognosis query like “What is the 5-year survival rate for stage III NSCLC with osimertinib?”, a gap fires if the retrieved evidence contains only mechanism discussions without a single survival figure. The gap propagates into the confidence score (described below) and into the final <code class="language-plaintext highlighter-rouge">DecisionOutput</code>, where it is visible to the user.</p>

<p><strong>A common false positive:</strong> A paper that reports percentage values in a non-statistical context, for example, “57% of patients were female” matches <code class="language-plaintext highlighter-rouge">\d+\.?\d*%</code> and suppresses the quantitative gap. This is tolerable: a paper that reports population percentages is also likely to contain outcome statistics elsewhere in its text.</p>

<h3 id="gap-3-contradictory-evidence">Gap 3: Contradictory Evidence</h3>

<p>If any evidence node is classified <code class="language-plaintext highlighter-rouge">contradictory</code>, that contradiction is surfaced as an explicit gap. This is not a retrieval failure — it is a signal about the state of the literature.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">contradictions</span> <span class="o">=</span> <span class="p">[</span><span class="n">e</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span> <span class="k">if</span> <span class="n">e</span><span class="p">.</span><span class="n">support_type</span> <span class="o">==</span> <span class="s">"contradictory"</span><span class="p">]</span>
<span class="k">if</span> <span class="n">contradictions</span><span class="p">:</span>
    <span class="n">gaps</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
        <span class="sa">f</span><span class="s">"Contradictory evidence present in </span><span class="si">{</span><span class="nb">len</span><span class="p">(</span><span class="n">contradictions</span><span class="p">)</span><span class="si">}</span><span class="s"> source(s) — "</span>
        <span class="s">"answer may reflect incomplete consensus"</span>
    <span class="p">)</span>
</code></pre></div></div>

<p><strong>Why this matters for clinical use:</strong> A system that silently synthesises an answer from three direct-evidence nodes and one contradictory node is a liability. A clinician who receives a confident answer has no reason to suspect that one of the four source papers reports null findings. The contradiction gap ensures this is always visible. It also directly reduces the confidence score, which is described in detail below.</p>

<h3 id="gap-4-low-relevance-scores">Gap 4: Low Relevance Scores</h3>

<p>BM25 retrieves top-K chunks regardless of whether any of them are actually relevant. If no document in the corpus addresses the query, BM25 still returns 15 results and they are just the best available match from an irrelevant corpus.</p>

<p>The gap detector catches this by checking whether the top raw BM25 result scores above a corpus-size-adjusted threshold. The threshold is derived from the IDF of a rare term — a term appearing in only one document out of N gets an IDF of approximately <code class="language-plaintext highlighter-rouge">log((N + 0.5) / 1.5 + 1)</code>. If the top result’s BM25 score is below <code class="language-plaintext highlighter-rouge">1.5 × idf_rare</code>, not even a single rare term has matched strongly, which means the corpus very likely does not contain the answer.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">all_results</span><span class="p">:</span>
    <span class="n">n</span> <span class="o">=</span> <span class="nb">max</span><span class="p">(</span><span class="n">corpus_chunk_count</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
    <span class="n">idf_rare</span> <span class="o">=</span> <span class="n">math</span><span class="p">.</span><span class="n">log</span><span class="p">((</span><span class="n">n</span> <span class="o">+</span> <span class="mf">0.5</span><span class="p">)</span> <span class="o">/</span> <span class="mf">1.5</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>
    <span class="n">low_score_threshold</span> <span class="o">=</span> <span class="n">idf_rare</span> <span class="o">*</span> <span class="mf">1.5</span>
    <span class="k">if</span> <span class="n">all_results</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">score</span> <span class="o">&lt;</span> <span class="n">low_score_threshold</span><span class="p">:</span>
        <span class="n">gaps</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
            <span class="s">"Retrieved evidence has low relevance scores — "</span>
            <span class="s">"the documents may not directly address this query"</span>
        <span class="p">)</span>
</code></pre></div></div>

<p>This threshold scales correctly as the corpus grows. A small corpus of 50 chunks has a lower IDF ceiling than a corpus of 5,000 chunks. The threshold rises as the index grows and a low-quality match becomes harder to hide behind corpus-size effects.</p>

<p><strong>The practical case this catches:</strong> A user ingests papers on cardiovascular disease and then asks about CRISPR-based cancer therapy. BM25 retrieves the best available CVD papers. Their scores are real scores — not zero — but they reflect only generic term overlap. The low-relevance gap fires, and the <code class="language-plaintext highlighter-rouge">DecisionOutput</code> tells the user to ingest topic-relevant papers before expecting a meaningful answer.</p>

<h3 id="gap-5-recency">Gap 5: Recency</h3>

<p>Biomedical evidence ages. A system that confidently synthesises an answer from 2003 papers about biomarker performance is missing a decade of follow-up validation and updated clinical guidelines.</p>

<p>The recency check is a heuristic: if at least half of the evidence nodes contain year-patterns matching the 1990s or early 2000s, a recency gap is flagged.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">old_docs</span> <span class="o">=</span> <span class="p">[</span><span class="n">e</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span> <span class="k">if</span> <span class="n">re</span><span class="p">.</span><span class="n">search</span><span class="p">(</span><span class="sa">r</span><span class="s">'\b(19|200[0-5])\d{2}\b'</span><span class="p">,</span> <span class="n">e</span><span class="p">.</span><span class="n">excerpt</span><span class="p">)]</span>
<span class="k">if</span> <span class="nb">len</span><span class="p">(</span><span class="n">old_docs</span><span class="p">)</span> <span class="o">&gt;=</span> <span class="nb">len</span><span class="p">(</span><span class="n">evidence</span><span class="p">)</span> <span class="o">//</span> <span class="mi">2</span> <span class="ow">and</span> <span class="n">evidence</span><span class="p">:</span>
    <span class="n">gaps</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
        <span class="s">"Evidence may be from older literature — consider verifying with recent publications"</span>
    <span class="p">)</span>
</code></pre></div></div>

<p>This is deliberately coarse. The correct long-term fix is to store the publication year as structured metadata on each document and compare it against the current date. Using regex on excerpt text means year-pattern matches can come from dates in study timelines rather than publication years. But for a zero-dependency core engine, a heuristic is preferable to introducing a structured metadata requirement.</p>

<h3 id="the-gap-list-as-a-first-class-output">The Gap List as a First-Class Output</h3>

<p>After running all five checks, <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector.detect_gaps()</code> returns a list capped at five gaps. This list is a first-class field in <code class="language-plaintext highlighter-rouge">DecisionOutput</code>, not a footnote. It feeds into the confidence formula and is displayed to the user alongside the answer.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gaps</span> <span class="o">=</span> <span class="bp">self</span><span class="p">.</span><span class="n">gap_detector</span><span class="p">.</span><span class="n">detect_gaps</span><span class="p">(</span>
    <span class="n">q_analysis</span><span class="p">,</span> <span class="n">evidence_nodes</span><span class="p">,</span> <span class="n">raw_results</span><span class="p">,</span> <span class="bp">self</span><span class="p">.</span><span class="n">index</span><span class="p">.</span><span class="n">doc_count</span>
<span class="p">)</span>
<span class="c1"># gaps might be:
# [
#   "Contradictory evidence present in 1 source(s) — answer may reflect incomplete consensus",
#   "Quantitative data not found in retrieved evidence",
# ]
</span></code></pre></div></div>

<p>Both gaps generated here will reduce the final confidence score, as described next.</p>

<hr />

<h2 id="stage-5-the-reasoning-chain-and-confidence-scoring">Stage 5: The Reasoning Chain and Confidence Scoring</h2>

<p><code class="language-plaintext highlighter-rouge">AnswerSynthesizer.synthesize()</code> has two jobs: build an explicit reasoning chain and compute a confidence score. These are connected — the reasoning chain documents the inputs that feed into the confidence formula, making the score interpretable rather than opaque.</p>

<h3 id="the-four-step-reasoning-chain">The Four-Step Reasoning Chain</h3>

<p>Every query produces a <code class="language-plaintext highlighter-rouge">reasoning_chain</code> — a list of <code class="language-plaintext highlighter-rouge">ReasoningStep</code> objects, each with a label, content string, and a step-level confidence score. The structure is fixed: steps 1 and 2 always execute; steps 3 and 4 only execute if there is evidence and gaps, respectively.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">@</span><span class="n">dataclass</span>
<span class="k">class</span> <span class="nc">ReasoningStep</span><span class="p">:</span>
    <span class="n">step_number</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">label</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">content</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">confidence</span><span class="p">:</span> <span class="nb">float</span>
</code></pre></div></div>

<p><strong>Step 1 — Query interpretation (confidence: 1.0)</strong></p>

<p>The query analyzer’s output is summarised: intent classification, named entities found, number of expanded tokens used in retrieval. Confidence is always 1.0 at this step because the query was parsed without information loss — even if the parse was incorrect, the parsing itself succeeded.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Query classified as 'diagnosis' intent.
Key entities identified: Alzheimer.
Search performed with 8 expanded tokens.
</code></pre></div></div>

<p>This step gives the user something to verify: if the intent was misclassified, they can see it here and formulate a corrected query.</p>

<p><strong>Step 2 — Evidence assessment (confidence: variable)</strong></p>

<p>The evidence count breakdown — direct, indirect, contradictory — is computed and its step-level confidence is calculated from the ratio of strong evidence to total:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">confidence</span> <span class="o">=</span> <span class="nb">min</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span>
    <span class="p">(</span><span class="n">direct_count</span> <span class="o">*</span> <span class="mf">0.3</span> <span class="o">+</span> <span class="n">indirect_count</span> <span class="o">*</span> <span class="mf">0.15</span><span class="p">)</span> <span class="o">/</span> <span class="nb">max</span><span class="p">(</span><span class="nb">len</span><span class="p">(</span><span class="n">evidence</span><span class="p">),</span> <span class="mi">1</span><span class="p">)</span> <span class="o">+</span> <span class="mf">0.4</span>
<span class="p">)</span>
</code></pre></div></div>

<p>At this step, the formula represents how strong the evidence pool is in isolation, before gaps are taken into account. Five direct evidence nodes yield approximately <code class="language-plaintext highlighter-rouge">(5 × 0.3) / 5 + 0.4 = 0.70</code>. Three direct and two indirect yield <code class="language-plaintext highlighter-rouge">(3 × 0.3 + 2 × 0.15) / 5 + 0.4 = 0.64</code>. The base of 0.4 ensures that even a weak evidence set starts with a non-zero assessment rather than collapsing to zero at this step.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Retrieved 5 evidence node(s): 2 direct, 2 indirect, 1 contradictory.
Sources span 4 document(s).
</code></pre></div></div>

<p><strong>Step 3 — Answer synthesis (confidence: variable)</strong></p>

<p>This step is set after the answer text is generated. Its confidence comes from <code class="language-plaintext highlighter-rouge">_compute_synthesis_confidence()</code>:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_compute_synthesis_confidence</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">evidence</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">EvidenceNode</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="n">avg_relevance</span> <span class="o">=</span> <span class="nb">sum</span><span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">relevance_score</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span><span class="p">)</span> <span class="o">/</span> <span class="nb">len</span><span class="p">(</span><span class="n">evidence</span><span class="p">)</span>
    <span class="n">direct_ratio</span> <span class="o">=</span> <span class="nb">sum</span><span class="p">(</span><span class="mi">1</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span> <span class="k">if</span> <span class="n">e</span><span class="p">.</span><span class="n">support_type</span> <span class="o">==</span> <span class="s">"direct"</span><span class="p">)</span> <span class="o">/</span> <span class="nb">len</span><span class="p">(</span><span class="n">evidence</span><span class="p">)</span>
    <span class="n">contra_penalty</span> <span class="o">=</span> <span class="nb">sum</span><span class="p">(</span><span class="mi">1</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span> <span class="k">if</span> <span class="n">e</span><span class="p">.</span><span class="n">support_type</span> <span class="o">==</span> <span class="s">"contradictory"</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.1</span>
    <span class="k">return</span> <span class="nb">max</span><span class="p">(</span><span class="mf">0.0</span><span class="p">,</span> <span class="nb">min</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">avg_relevance</span> <span class="o">*</span> <span class="mf">0.5</span> <span class="o">+</span> <span class="n">direct_ratio</span> <span class="o">*</span> <span class="mf">0.5</span> <span class="o">-</span> <span class="n">contra_penalty</span><span class="p">))</span>
</code></pre></div></div>

<p>Three signals here. <code class="language-plaintext highlighter-rouge">avg_relevance</code> is the mean of the normalized relevance scores across the five evidence nodes. It reflects how well the reranker scores the documents after all three penalty factors are applied. <code class="language-plaintext highlighter-rouge">direct_ratio</code> is the fraction of evidence that contains explicit statistical results or confirmatory language. Each contradictory evidence node subtracts a flat 0.1 penalty.</p>

<p>This formula is what actually penalises contradictions at the synthesis step. A set of five evidence nodes where three are direct, one is indirect, and one is contradictory, with average relevance of 0.85, yields: <code class="language-plaintext highlighter-rouge">0.85 × 0.5 + 0.6 × 0.5 − 0.1 = 0.625</code>. Replace the contradictory node with another direct node and it rises to <code class="language-plaintext highlighter-rouge">0.85 × 0.5 + 0.8 × 0.5 = 0.825</code>.</p>

<p><strong>Step 4 — Knowledge gap analysis (confidence: 0.7)</strong></p>

<p>If gaps were detected, they are summarised at this step. The step confidence is fixed at 0.7 — the system is fairly confident in its gap identification but acknowledges that the gap detector is heuristic and may miss gaps it could not pattern-match.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Identified 2 gap(s): Contradictory evidence present in 1 source(s) — answer may
reflect incomplete consensus; Quantitative data not found in retrieved evidence.
</code></pre></div></div>

<h3 id="the-overall-confidence-formula">The Overall Confidence Formula</h3>

<p>After the reasoning chain is assembled, the final confidence score is computed by <code class="language-plaintext highlighter-rouge">_compute_overall_confidence()</code>. This is the number that surfaces in <code class="language-plaintext highlighter-rouge">DecisionOutput.confidence</code> and drives the label (<code class="language-plaintext highlighter-rouge">"High"</code>, <code class="language-plaintext highlighter-rouge">"Moderate"</code>, <code class="language-plaintext highlighter-rouge">"Low"</code>, <code class="language-plaintext highlighter-rouge">"Insufficient"</code>).</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_compute_overall_confidence</span><span class="p">(</span>
    <span class="bp">self</span><span class="p">,</span>
    <span class="n">evidence</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">EvidenceNode</span><span class="p">],</span>
    <span class="n">gaps</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">],</span>
    <span class="n">direct_count</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="n">base</span> <span class="o">=</span> <span class="mf">0.3</span>
    <span class="n">evidence_bonus</span> <span class="o">=</span> <span class="nb">min</span><span class="p">(</span><span class="mf">0.4</span><span class="p">,</span> <span class="nb">len</span><span class="p">(</span><span class="n">evidence</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.08</span><span class="p">)</span>
    <span class="n">direct_bonus</span> <span class="o">=</span> <span class="nb">min</span><span class="p">(</span><span class="mf">0.2</span><span class="p">,</span> <span class="n">direct_count</span> <span class="o">*</span> <span class="mf">0.07</span><span class="p">)</span>
    <span class="n">gap_penalty</span> <span class="o">=</span> <span class="nb">len</span><span class="p">(</span><span class="n">gaps</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.05</span>
    <span class="k">return</span> <span class="nb">max</span><span class="p">(</span><span class="mf">0.05</span><span class="p">,</span> <span class="nb">min</span><span class="p">(</span><span class="mf">0.95</span><span class="p">,</span> <span class="n">base</span> <span class="o">+</span> <span class="n">evidence_bonus</span> <span class="o">+</span> <span class="n">direct_bonus</span> <span class="o">-</span> <span class="n">gap_penalty</span><span class="p">))</span>
</code></pre></div></div>

<p>Four inputs, each with a bounded contribution:</p>

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Effect</th>
      <th>Range</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Evidence count</td>
      <td>+0.08 per node, capped at +0.40</td>
      <td>0 to 0.40</td>
    </tr>
    <tr>
      <td>Direct evidence count</td>
      <td>+0.07 per direct node, capped at +0.20</td>
      <td>0 to 0.20</td>
    </tr>
    <tr>
      <td>Gap count</td>
      <td>−0.05 per gap</td>
      <td>uncapped downward</td>
    </tr>
    <tr>
      <td>Base</td>
      <td>always 0.30</td>
      <td>—</td>
    </tr>
  </tbody>
</table>

<p>The maximum achievable score is 0.95 (<code class="language-plaintext highlighter-rouge">max(0.05, min(0.95, ...))</code>). The formula never returns 0.0 or 1.0, which is intentional. Absolute zero confidence would mean the system refuses to respond at all; absolute certainty is epistemically dishonest in biomedical literature synthesis. The ceiling of 0.95 is a statement that a probabilistic system over noisy biomedical text should never claim to be certain.</p>

<p><strong>Worked examples:</strong></p>

<p><em>Strong evidence case:</em> 5 evidence nodes, 4 direct, 0 gaps.
<code class="language-plaintext highlighter-rouge">0.3 + min(0.4, 5 × 0.08) + min(0.2, 4 × 0.07) − 0 = 0.3 + 0.4 + 0.2 = 0.90 → "High"</code></p>

<p><em>Weak evidence case:</em> 3 evidence nodes, 1 direct, 3 gaps (contradiction, no stats, low relevance).
<code class="language-plaintext highlighter-rouge">0.3 + min(0.4, 3 × 0.08) + min(0.2, 1 × 0.07) − (3 × 0.05) = 0.3 + 0.24 + 0.07 − 0.15 = 0.46 → "Moderate"</code></p>

<p><em>Corpus miss case:</em> 2 evidence nodes, 0 direct, 4 gaps (entity missing, low relevance, no stats, recency).
<code class="language-plaintext highlighter-rouge">0.3 + min(0.4, 2 × 0.08) + 0 − (4 × 0.05) = 0.3 + 0.16 − 0.2 = 0.26 → "Low"</code></p>

<h3 id="why-calibrated-beats-high">Why Calibrated Beats High</h3>

<p>The temptation in any retrieval system is to make confidence scores look high. High-confidence answers feel more useful. But in a clinical context, a miscalibrated high-confidence answer that rests on weak evidence is more dangerous than a correctly low-confidence answer that tells the user to seek additional evidence.</p>

<p>BioRAG’s formula is deliberately conservative. The base of 0.3 means an answer from zero direct evidence and zero gaps starts at 30% — <code class="language-plaintext highlighter-rouge">"Low"</code>. You need 4+ evidence nodes with 2+ direct classifications to breach <code class="language-plaintext highlighter-rouge">"High"</code>. This is not a design flaw; it is a design choice: the system should default to skepticism and require evidence to move the needle upward.</p>

<p>The consequence is that <code class="language-plaintext highlighter-rouge">"Moderate"</code> is the most common label in practice, which is correct for a system answering biomedical research questions from an indexed corpus that is always narrower than the published literature.</p>

<hr />

<h2 id="the-answer-synthesizer-building-the-prose">The Answer Synthesizer: Building the Prose</h2>

<p>After confidence is computed, <code class="language-plaintext highlighter-rouge">_build_answer()</code> assembles the actual response text from the evidence nodes. The rule-based synthesizer — the core engine default — constructs the answer in three passes.</p>

<p><strong>Pass 1: Primary evidence summary</strong></p>

<p>Direct evidence nodes are listed first, up to three, with each excerpt truncated at a sentence boundary to stay under 300 characters. If no direct evidence exists, the three best indirect nodes are used instead, with a framing caveat. If neither exists, the answer states that evidence is insufficient.</p>

<p>The sentence-boundary truncation is worth emphasising. Cutting at 300 raw characters would routinely end mid-sentence, creating excerpts that are syntactically incomplete and misleading. <code class="language-plaintext highlighter-rouge">TextProcessor.truncate_at_sentence()</code> scans forward from 300 characters to the next sentence boundary, ensuring that every displayed excerpt is a complete claim.</p>

<p><strong>Pass 2: Contradiction warning</strong></p>

<p>If any contradictory nodes exist, they are surfaced separately with an explicit warning marker. In the rule-based synthesizer this is the <code class="language-plaintext highlighter-rouge">⚠</code> prefix. In the LLM synthesizer (described next), the model is instructed to acknowledge the contradiction in-line with a citation.</p>

<p><strong>Pass 3: Cross-source convergence</strong></p>

<p>If two or more evidence nodes share key terms like biomarker names, gene identifiers, statistical values, then <code class="language-plaintext highlighter-rouge">_find_common_terms()</code> surfaces those as a convergence signal:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_find_common_terms</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">evidence</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">EvidenceNode</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
    <span class="n">term_counts</span><span class="p">:</span> <span class="n">Counter</span> <span class="o">=</span> <span class="n">Counter</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span><span class="p">:</span>
        <span class="n">term_counts</span><span class="p">.</span><span class="n">update</span><span class="p">(</span><span class="nb">set</span><span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">key_terms</span><span class="p">))</span>
    <span class="k">return</span> <span class="p">[</span><span class="n">term</span> <span class="k">for</span> <span class="n">term</span><span class="p">,</span> <span class="n">count</span> <span class="ow">in</span> <span class="n">term_counts</span><span class="p">.</span><span class="n">most_common</span><span class="p">(</span><span class="mi">6</span><span class="p">)</span> <span class="k">if</span> <span class="n">count</span> <span class="o">&gt;=</span> <span class="mi">2</span><span class="p">]</span>
</code></pre></div></div>

<p>A term appearing in three out of five evidence nodes is a signal worth surfacing. It is not proof of consensus as two papers can cite the same term in opposite conclusions, but repeated mention across independent sources is at minimum a flag that this term is centrally relevant to the question.</p>

<hr />

<h2 id="the-llm-answer-synthesizer">The LLM Answer Synthesizer</h2>

<p>The rule-based <code class="language-plaintext highlighter-rouge">_build_answer()</code> is adequate for surfacing evidence, but it is structured text assembly. For users who want a natural language answer, BioRAG provides <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code>, a drop-in replacement that swaps only <code class="language-plaintext highlighter-rouge">_build_answer()</code> for a Claude API call.</p>

<p>Critically: every other part of the pipeline is identical. The reasoning chain construction, confidence scoring, gap detection, and evidence classification all run through the same code path. The LLM only touches the final prose generation step.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">engine</span> <span class="o">=</span> <span class="n">BioRAGEngine</span><span class="p">(</span><span class="n">synthesizer</span><span class="o">=</span><span class="n">ClaudeSynthesizer</span><span class="p">())</span>
<span class="n">result</span> <span class="o">=</span> <span class="n">engine</span><span class="p">.</span><span class="n">query</span><span class="p">(</span><span class="s">"What plasma biomarkers predict Alzheimer's disease?"</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="the-grounding-contract">The Grounding Contract</h3>

<p>The system prompt enforces a strict grounding contract:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Every factual claim in your answer MUST be traceable to at least one of
the supplied excerpts. Cite sources inline as [N] where N is the excerpt number.
Do NOT use knowledge from your training data. If the excerpts do not contain
sufficient information, say so explicitly.
</code></pre></div></div>

<p>This is the core safety property of the LLM synthesizer. A model that draws on training knowledge to fill gaps in the retrieved evidence is a hallucination risk in exactly the contexts where hallucinations are most harmful. The system prompt forbids it explicitly and instructs the model to surface evidence insufficiency as a failure mode rather than paper over it.</p>

<h3 id="evidence-formatting-in-the-user-message">Evidence Formatting in the User Message</h3>

<p>The user message passes each evidence node as a numbered excerpt with metadata:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1] Plasma Aβ42/p-Tau217 ratio as a blood-based biomarker...
    | Section: Results | Relevance: 100% | Type: DIRECT
Excerpt: Plasma pTau217 demonstrated an AUC of 0.94 (95% CI 0.91–0.97)...

[2] Diagnostic Blood-Based Biomarkers for Alzheimer's Disease
    | Section: Results | Relevance: 83% | Type: DIRECT
Excerpt: Amyloid-β42/40 ratio and NfL showed significant associations...

[3] Longitudinal Changes in Plasma Biomarkers...
    | Section: Discussion | Relevance: 64% | Type: CONTRADICTORY
Excerpt: However, plasma biomarker sensitivity failed to reach clinical...
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">DIRECT</code> / <code class="language-plaintext highlighter-rouge">INDIRECT</code> / <code class="language-plaintext highlighter-rouge">CONTRADICTORY</code> type label is included in the excerpt header so Claude knows how to weight each source before generating prose. A CONTRADICTORY source should be acknowledged differently than a DIRECT one.</p>

<h3 id="prompt-caching">Prompt Caching</h3>

<p>The system prompt, which is long and static, is cached with <code class="language-plaintext highlighter-rouge">"cache_control": {"type": "ephemeral"}</code>. This reduces API latency and cost for repeated queries in the same session because the system prompt tokens are only processed once.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">system</span><span class="o">=</span><span class="p">[</span>
    <span class="p">{</span>
        <span class="s">"type"</span><span class="p">:</span> <span class="s">"text"</span><span class="p">,</span>
        <span class="s">"text"</span><span class="p">:</span> <span class="n">SYSTEM_PROMPT</span><span class="p">,</span>
        <span class="s">"cache_control"</span><span class="p">:</span> <span class="p">{</span><span class="s">"type"</span><span class="p">:</span> <span class="s">"ephemeral"</span><span class="p">},</span>
    <span class="p">}</span>
<span class="p">]</span>
</code></pre></div></div>

<p>For a decision-support session where a clinician is iterating through a series of related queries, prompt caching cuts the effective token cost of the system prompt to near zero after the first query in a session.</p>

<h3 id="fallback-on-api-failure">Fallback on API Failure</h3>

<p>If the Claude API call fails for any reason — network error, rate limit, model unavailability — <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> falls back to the parent class’s rule-based <code class="language-plaintext highlighter-rouge">_build_answer()</code> and appends a note to the reasoning chain indicating fallback occurred. The confidence score and gap list are unaffected.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">exc</span><span class="p">:</span>
    <span class="n">answer_parts</span> <span class="o">=</span> <span class="bp">self</span><span class="p">.</span><span class="n">_build_answer</span><span class="p">(</span><span class="n">query_analysis</span><span class="p">,</span> <span class="n">evidence</span><span class="p">)</span>
    <span class="n">answer_text</span> <span class="o">=</span> <span class="s">"</span><span class="se">\n\n</span><span class="s">"</span><span class="p">.</span><span class="n">join</span><span class="p">(</span><span class="n">answer_parts</span><span class="p">)</span>
    <span class="n">synthesis_note</span> <span class="o">=</span> <span class="sa">f</span><span class="s">"LLM synthesis failed (</span><span class="si">{</span><span class="n">exc</span><span class="si">}</span><span class="s">); used rule-based fallback."</span>
</code></pre></div></div>

<p>This fallback is a design requirement. A clinical decision-support system that becomes unusable when an external API is unavailable is not suitable for production use.</p>

<hr />

<h2 id="stage-6-the-follow-up-generator">Stage 6: The Follow-Up Generator</h2>

<p>After the answer is synthesised, <code class="language-plaintext highlighter-rouge">FollowUpGenerator.generate()</code> produces up to four follow-up questions. The goal is not to prompt the user with generic next questions, but, to move them from a query that the system answered imperfectly toward a query that is more precisely answerable.</p>

<h3 id="intent-aware-templates">Intent-Aware Templates</h3>

<p>The follow-up templates are keyed to the query’s intent classification. A <code class="language-plaintext highlighter-rouge">diagnosis</code> query and a <code class="language-plaintext highlighter-rouge">treatment</code> query need very different follow-ups:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">TEMPLATES</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"diagnosis"</span><span class="p">:</span> <span class="p">[</span>
        <span class="s">"What is the sensitivity and specificity of this diagnostic approach?"</span><span class="p">,</span>
        <span class="s">"What are the differential diagnoses to consider?"</span><span class="p">,</span>
        <span class="s">"Are there validated clinical scoring systems for this condition?"</span><span class="p">,</span>
    <span class="p">],</span>
    <span class="s">"treatment"</span><span class="p">:</span> <span class="p">[</span>
        <span class="s">"What is the recommended dosage range and administration route?"</span><span class="p">,</span>
        <span class="s">"What are the most common adverse effects reported?"</span><span class="p">,</span>
        <span class="s">"Is there evidence of treatment resistance or tolerance?"</span><span class="p">,</span>
    <span class="p">],</span>
    <span class="s">"mechanism"</span><span class="p">:</span> <span class="p">[</span>
        <span class="s">"What are the downstream effects of {entity}?"</span><span class="p">,</span>
        <span class="s">"Which molecular targets are involved in this pathway?"</span><span class="p">,</span>
        <span class="s">"Are there known inhibitors or activators of this mechanism?"</span><span class="p">,</span>
    <span class="p">],</span>
    <span class="c1"># ...
</span><span class="p">}</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">{entity}</code> placeholder is filled with the first entity extracted by the <code class="language-plaintext highlighter-rouge">QueryAnalyzer</code>. For a query about “BRCA1 mutation mechanisms”, the first follow-up becomes “What are the downstream effects of BRCA1?” — a question that narrows from general mechanism to consequence.</p>

<h3 id="gap-driven-questions">Gap-Driven Questions</h3>

<p>The fourth follow-up question is generated from the key terms shared across the top two evidence nodes:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">key_terms</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">evidence</span><span class="p">[:</span><span class="mi">2</span><span class="p">]:</span>
    <span class="n">key_terms</span><span class="p">.</span><span class="n">extend</span><span class="p">(</span><span class="n">e</span><span class="p">.</span><span class="n">key_terms</span><span class="p">[:</span><span class="mi">2</span><span class="p">])</span>
<span class="n">unique_terms</span> <span class="o">=</span> <span class="nb">list</span><span class="p">(</span><span class="nb">dict</span><span class="p">.</span><span class="n">fromkeys</span><span class="p">(</span><span class="n">key_terms</span><span class="p">))[:</span><span class="mi">2</span><span class="p">]</span>
<span class="n">questions</span><span class="p">.</span><span class="n">append</span><span class="p">(</span>
    <span class="sa">f</span><span class="s">"What is the clinical significance of </span><span class="si">{</span><span class="s">' and '</span><span class="p">.</span><span class="n">join</span><span class="p">(</span><span class="n">unique_terms</span><span class="p">)</span><span class="si">}</span><span class="s">?"</span>
<span class="p">)</span>
</code></pre></div></div>

<p>This is the follow-up generator’s most practical contribution. If the system retrieved evidence about “pTau217” and “amyloid-β42/40”, the follow-up is “What is the clinical significance of pTau217 and amyloid-β42/40?”. This is gap-driven in the sense that it surfaces the entities the system found most relevant, inviting the user to go deeper on those specific signals.</p>

<hr />

<h2 id="what-the-full-pipeline-produces">What the Full Pipeline Produces</h2>

<p>At the end of all six stages, <code class="language-plaintext highlighter-rouge">BioRAGEngine.query()</code> returns a <code class="language-plaintext highlighter-rouge">DecisionOutput</code>:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">@</span><span class="n">dataclass</span>
<span class="k">class</span> <span class="nc">DecisionOutput</span><span class="p">:</span>
    <span class="n">query</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">answer</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">confidence</span><span class="p">:</span> <span class="nb">float</span>
    <span class="n">confidence_label</span><span class="p">:</span> <span class="nb">str</span>          <span class="c1"># "High" | "Moderate" | "Low" | "Insufficient"
</span>    <span class="n">evidence</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">EvidenceNode</span><span class="p">]</span>
    <span class="n">reasoning_chain</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">ReasoningStep</span><span class="p">]</span>
    <span class="n">knowledge_gaps</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]</span>
    <span class="n">follow_up_questions</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]</span>
    <span class="n">sources_used</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">total_chunks_searched</span><span class="p">:</span> <span class="nb">int</span>
</code></pre></div></div>

<p>For the query “What plasma biomarkers predict Alzheimer’s disease?”, a well-indexed corpus produces something like this:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">DecisionOutput</span><span class="p">(</span>
    <span class="n">query</span><span class="o">=</span><span class="s">"What plasma biomarkers predict Alzheimer's disease?"</span><span class="p">,</span>
    <span class="n">confidence</span><span class="o">=</span><span class="mf">0.81</span><span class="p">,</span>
    <span class="n">confidence_label</span><span class="o">=</span><span class="s">"High"</span><span class="p">,</span>
    <span class="n">sources_used</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span>
    <span class="n">total_chunks_searched</span><span class="o">=</span><span class="mi">312</span><span class="p">,</span>

    <span class="n">evidence</span><span class="o">=</span><span class="p">[</span>
        <span class="n">EvidenceNode</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Plasma Aβ42/p-Tau217 ratio..."</span><span class="p">,</span>
                     <span class="n">section</span><span class="o">=</span><span class="s">"Results"</span><span class="p">,</span> <span class="n">relevance_score</span><span class="o">=</span><span class="mf">1.000</span><span class="p">,</span>
                     <span class="n">support_type</span><span class="o">=</span><span class="s">"direct"</span><span class="p">,</span>
                     <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"pTau217"</span><span class="p">,</span> <span class="s">"AUC"</span><span class="p">,</span> <span class="s">"0.94"</span><span class="p">,</span> <span class="s">"95% CI"</span><span class="p">,</span> <span class="s">"Alzheimer"</span><span class="p">]),</span>
        <span class="n">EvidenceNode</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Diagnostic Blood-Based Biomarkers..."</span><span class="p">,</span>
                     <span class="n">section</span><span class="o">=</span><span class="s">"Results"</span><span class="p">,</span> <span class="n">relevance_score</span><span class="o">=</span><span class="mf">0.832</span><span class="p">,</span>
                     <span class="n">support_type</span><span class="o">=</span><span class="s">"direct"</span><span class="p">,</span>
                     <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"Amyloid"</span><span class="p">,</span> <span class="s">"NfL"</span><span class="p">,</span> <span class="s">"significant"</span><span class="p">,</span> <span class="s">"Alzheimer"</span><span class="p">]),</span>
        <span class="n">EvidenceNode</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Longitudinal Changes in Plasma Biomarkers..."</span><span class="p">,</span>
                     <span class="n">section</span><span class="o">=</span><span class="s">"Discussion"</span><span class="p">,</span> <span class="n">relevance_score</span><span class="o">=</span><span class="mf">0.641</span><span class="p">,</span>
                     <span class="n">support_type</span><span class="o">=</span><span class="s">"contradictory"</span><span class="p">,</span>
                     <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"plasma"</span><span class="p">,</span> <span class="s">"sensitivity"</span><span class="p">,</span> <span class="s">"failed"</span><span class="p">,</span> <span class="s">"clinical threshold"</span><span class="p">]),</span>
    <span class="p">],</span>

    <span class="n">reasoning_chain</span><span class="o">=</span><span class="p">[</span>
        <span class="n">ReasoningStep</span><span class="p">(</span><span class="n">step_number</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="s">"Query interpretation"</span><span class="p">,</span>
                      <span class="n">content</span><span class="o">=</span><span class="s">"Query classified as 'diagnosis' intent. "</span>
                              <span class="s">"Key entities: Alzheimer. "</span>
                              <span class="s">"Search performed with 8 expanded tokens."</span><span class="p">,</span>
                      <span class="n">confidence</span><span class="o">=</span><span class="mf">1.0</span><span class="p">),</span>
        <span class="n">ReasoningStep</span><span class="p">(</span><span class="n">step_number</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="s">"Evidence assessment"</span><span class="p">,</span>
                      <span class="n">content</span><span class="o">=</span><span class="s">"Retrieved 5 node(s): 2 direct, 2 indirect, 1 contradictory. "</span>
                              <span class="s">"Sources span 4 document(s)."</span><span class="p">,</span>
                      <span class="n">confidence</span><span class="o">=</span><span class="mf">0.64</span><span class="p">),</span>
        <span class="n">ReasoningStep</span><span class="p">(</span><span class="n">step_number</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="s">"Answer synthesis"</span><span class="p">,</span>
                      <span class="n">content</span><span class="o">=</span><span class="s">"Synthesized from 5 excerpt(s). "</span>
                              <span class="s">"Primary: 'Plasma Aβ42/p-Tau217 ratio' (Results). "</span>
                              <span class="s">"Note: contradictory evidence in corpus."</span><span class="p">,</span>
                      <span class="n">confidence</span><span class="o">=</span><span class="mf">0.68</span><span class="p">),</span>
        <span class="n">ReasoningStep</span><span class="p">(</span><span class="n">step_number</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="s">"Knowledge gap analysis"</span><span class="p">,</span>
                      <span class="n">content</span><span class="o">=</span><span class="s">"Identified 1 gap(s): Contradictory evidence present..."</span><span class="p">,</span>
                      <span class="n">confidence</span><span class="o">=</span><span class="mf">0.7</span><span class="p">),</span>
    <span class="p">],</span>

    <span class="n">knowledge_gaps</span><span class="o">=</span><span class="p">[</span>
        <span class="s">"Contradictory evidence present in 1 source(s) — "</span>
        <span class="s">"answer may reflect incomplete consensus"</span>
    <span class="p">],</span>

    <span class="n">follow_up_questions</span><span class="o">=</span><span class="p">[</span>
        <span class="s">"What is the sensitivity and specificity of this diagnostic approach?"</span><span class="p">,</span>
        <span class="s">"What are the differential diagnoses to consider?"</span><span class="p">,</span>
        <span class="s">"Are there validated clinical scoring systems for this condition?"</span><span class="p">,</span>
        <span class="s">"What is the clinical significance of pTau217 and Amyloid?"</span><span class="p">,</span>
    <span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div>

<p>Every field is auditable. The confidence label “High” is traceable to the formula: <code class="language-plaintext highlighter-rouge">0.3 + 0.4 + 0.14 − 0.05 = 0.79</code> (5 nodes, 2 direct, 1 gap). The contradictory evidence node is visible in <code class="language-plaintext highlighter-rouge">evidence</code> and referenced in both <code class="language-plaintext highlighter-rouge">reasoning_chain</code> step 3 and <code class="language-plaintext highlighter-rouge">knowledge_gaps</code>. The follow-up questions are grounded in both the query’s classified intent and the actual terms found in the evidence.</p>

<p>Nothing is hidden. Nothing is inferred silently.</p>

<hr />

<h2 id="limitations-and-how-to-improve">Limitations and How to Improve</h2>

<h3 id="the-rule-based-synthesizer-is-not-a-prose-writer">The Rule-Based Synthesizer Is Not a Prose Writer</h3>

<p><code class="language-plaintext highlighter-rouge">_build_answer()</code> produces structured text, not analysis. It concatenates excerpts; it does not argue from them. For a researcher who needs a synthesised interpretation — not just a list of relevant passages — the rule-based synthesizer is a starting point, not a destination. The <code class="language-plaintext highlighter-rouge">ClaudeSynthesizer</code> addresses this, but at the cost of an external API dependency.</p>

<p><strong>Short-term improvement:</strong> Add a cross-source comparison step to <code class="language-plaintext highlighter-rouge">_build_answer()</code>. When two direct-evidence nodes report different numerical values for the same endpoint (e.g., one AUC of 0.94 and another of 0.81 for plasma pTau217), note the discrepancy explicitly rather than listing both neutrally.</p>

<h3 id="gap-detection-is-heuristic">Gap Detection Is Heuristic</h3>

<p>All five gap checks rely on pattern matching over text. The quantitative gap check catches <code class="language-plaintext highlighter-rouge">\d+\.?\d*%</code> and misses narratively described results (“a three-fold reduction”). The recency check matches year patterns in excerpt text rather than structured publication metadata. The entity coverage check is case-sensitive and fails when the corpus uses different capitalisation or abbreviation conventions than the query.</p>

<p><strong>Medium-term improvement:</strong> Ingest structured metadata — publication year, journal, study design — alongside document text, and run gap checks against those structured fields rather than regex on excerpts. This also enables a new gap type: study-design gap (all retrieved evidence is from observational studies when the query asks about RCT evidence).</p>

<h3 id="confidence-is-uncalibrated">Confidence Is Uncalibrated</h3>

<p>The formula produces a score between 0.05 and 0.95, but there is no ground truth against which to calibrate it. A confidence of 0.81 should ideally mean the system is correct 81% of the time on queries that produce that score. Without a labelled answer-quality eval set, that claim cannot be verified.</p>

<p><strong>Long-term improvement:</strong> Build an answer-quality evaluation harness alongside the existing retrieval eval harness (covered in Post 5). For a set of queries with known correct answers, run the full pipeline and measure whether high-confidence answers are more often correct than low-confidence ones. If the curve is not monotonic, the formula coefficients need adjustment. This is calibration in the statistical sense — it is harder to implement than retrieval eval but more important for clinical use.</p>

<h3 id="followupgenerator-is-lookup-based"><code class="language-plaintext highlighter-rouge">FollowUpGenerator</code> Is Lookup-Based</h3>

<p>The template lookup produces reasonable follow-ups for common intents, but it has no awareness of what was actually missing from the evidence. A gap-driven follow-up that says “consider searching for quantitative effect sizes” is more useful than “what patient populations were studied?” when the quantitative gap fired.</p>

<p><strong>Short-term improvement:</strong> Use the detected gaps as direct inputs to follow-up generation. If the quantitative gap fired, prepend “What are the reported effect sizes and confidence intervals for this finding?” as the first follow-up, regardless of intent template. If the entity coverage gap fired, generate a follow-up that names the missing entity: “What evidence exists specifically for {missing entity}?”</p>

<hr />

<h2 id="coming-up-in-post-4">Coming Up in Post 4</h2>

<p>Post 4 covers the full LLM answer synthesis path in depth. We will look at prompt engineering decisions — why the system prompt forbids training knowledge use, what makes the evidence formatting in the user message effective, and how to test that the grounding constraint is actually holding.</p>

<p>We will trace a failure case where the model partially violated the grounding rule despite the instruction — what the output looked like, how it was detected, and what prompt change fixed it.</p>

<p>We will also look at how the full pipeline integrates with the FastAPI server (<code class="language-plaintext highlighter-rouge">server.py</code>) and the MCP server (<code class="language-plaintext highlighter-rouge">mcp_server.py</code>), and what it means in practice to call BioRAG as a tool from within a Claude Code session using the registered MCP interface.</p>

<hr />

<p><em>BioRAG is built in pure Python stdlib. The core engine — BM25 index, reranker, evidence classifier, synthesizer — has zero external dependencies. PubMed ingestion requires only <code class="language-plaintext highlighter-rouge">requests</code>. LLM answer synthesis requires only <code class="language-plaintext highlighter-rouge">anthropic</code>.</em></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Building BioRAG: A Decision-Support RAG System for Biomedical Literature]]></summary></entry><entry><title type="html">BioRAG: Part 2</title><link href="/2026/05/15/BioRAG-part2-reranking-evidence-classification.html" rel="alternate" type="text/html" title="BioRAG: Part 2" /><published>2026-05-15T00:00:00+10:00</published><updated>2026-05-15T00:00:00+10:00</updated><id>/2026/05/15/BioRAG-part2-reranking-evidence-classification</id><content type="html" xml:base="/2026/05/15/BioRAG-part2-reranking-evidence-classification.html"><![CDATA[<h1 id="biorag">BioRAG</h1>

<p><strong>Part 2 of 5 — Reranking, Evidence Classification, and False Positive Suppression</strong></p>

<hr />

<h2 id="where-we-left-off">Where We Left Off</h2>

<p>Post 1 ended with 15 BM25 candidates sitting in a list. Most were good. One was a lung cancer paper that had muscled its way into rank 9 of an Alzheimer’s disease query. The culprit was legitimate BM25 scoring. It used “biomarker” and “disease” heavily. It never mentioned Alzheimer’s once.</p>

<p>That paper is the problem this post solves.</p>

<p>By the end of Post 1, the pipeline had produced this:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">retrieved_chunks</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">RetrievedChunk</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Plasma Aβ42/p-Tau217 ratio..."</span><span class="p">,</span>       <span class="n">score</span><span class="o">=</span><span class="mf">9.72</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
    <span class="n">RetrievedChunk</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Diagnostic Blood-Based Biomarkers..."</span><span class="p">,</span> <span class="n">score</span><span class="o">=</span><span class="mf">8.10</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span>
    <span class="n">RetrievedChunk</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"EGFR-Mutant Non-Small Cell Lung..."</span><span class="p">,</span>   <span class="n">score</span><span class="o">=</span><span class="mf">7.30</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">9</span><span class="p">),</span>
    <span class="c1"># ...
</span><span class="p">]</span>
</code></pre></div></div>

<p>This post covers the two stages that turn those 15 raw candidates into 5 high-quality evidence nodes: the <strong>Reranker</strong> and the <strong>EvidenceClassifier</strong>.</p>

<hr />

<h2 id="the-reranker-three-signals-on-top-of-bm25">The Reranker: Three Signals on Top of BM25</h2>

<p>The <code class="language-plaintext highlighter-rouge">Reranker</code> does not replace BM25. It applies three additional signals to adjust the scores BM25 already produced and keeps only the top-K results. The three signals are section weighting, term density, and discriminative-token recall.</p>

<h3 id="signal-1-section-weighting">Signal 1: Section Weighting</h3>

<p>Not all sections of a biomedical paper are equally valuable for a given query intent. A mechanism query wants the Methods and Results sections — those sections contain the pathway experiments and quantitative findings. A definition query wants the Introduction or Abstract. A treatment query wants Results and Discussion.</p>

<p>The <code class="language-plaintext highlighter-rouge">Reranker</code> encodes this as a lookup table:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">SECTION_WEIGHTS</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"mechanism"</span><span class="p">:</span> <span class="p">{</span><span class="s">"Results"</span><span class="p">:</span> <span class="mf">1.3</span><span class="p">,</span> <span class="s">"Methods"</span><span class="p">:</span> <span class="mf">1.1</span><span class="p">,</span> <span class="s">"Discussion"</span><span class="p">:</span> <span class="mf">1.0</span><span class="p">,</span> <span class="s">"Abstract"</span><span class="p">:</span> <span class="mf">0.9</span><span class="p">},</span>
    <span class="s">"comparison"</span><span class="p">:</span> <span class="p">{</span><span class="s">"Results"</span><span class="p">:</span> <span class="mf">1.4</span><span class="p">,</span> <span class="s">"Discussion"</span><span class="p">:</span> <span class="mf">1.2</span><span class="p">,</span> <span class="s">"Abstract"</span><span class="p">:</span> <span class="mf">1.0</span><span class="p">},</span>
    <span class="s">"treatment"</span><span class="p">:</span>  <span class="p">{</span><span class="s">"Results"</span><span class="p">:</span> <span class="mf">1.4</span><span class="p">,</span> <span class="s">"Methods"</span><span class="p">:</span> <span class="mf">1.2</span><span class="p">,</span> <span class="s">"Discussion"</span><span class="p">:</span> <span class="mf">1.0</span><span class="p">},</span>
    <span class="s">"diagnosis"</span><span class="p">:</span>  <span class="p">{</span><span class="s">"Results"</span><span class="p">:</span> <span class="mf">1.3</span><span class="p">,</span> <span class="s">"Methods"</span><span class="p">:</span> <span class="mf">1.2</span><span class="p">,</span> <span class="s">"Abstract"</span><span class="p">:</span> <span class="mf">1.0</span><span class="p">},</span>
    <span class="s">"definition"</span><span class="p">:</span> <span class="p">{</span><span class="s">"Introduction"</span><span class="p">:</span> <span class="mf">1.3</span><span class="p">,</span> <span class="s">"Abstract"</span><span class="p">:</span> <span class="mf">1.2</span><span class="p">,</span> <span class="s">"Body"</span><span class="p">:</span> <span class="mf">1.0</span><span class="p">},</span>
    <span class="s">"general"</span><span class="p">:</span>    <span class="p">{</span><span class="s">"Abstract"</span><span class="p">:</span> <span class="mf">1.1</span><span class="p">,</span> <span class="s">"Results"</span><span class="p">:</span> <span class="mf">1.1</span><span class="p">,</span> <span class="s">"Discussion"</span><span class="p">:</span> <span class="mf">1.0</span><span class="p">},</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Each chunk already carries a <code class="language-plaintext highlighter-rouge">section</code> field that is set at chunking time by <code class="language-plaintext highlighter-rouge">DocumentChunker._detect_section()</code>. This scans the first line of each chunk for header patterns. A chunk from a Results section with a diagnosis query gets a 1.3× multiplier. A chunk from an undetected section falls back to 1.0.</p>

<p><strong>What this buys you:</strong> For a query like “What plasma biomarkers detect Alzheimer’s disease?”, a Results chunk reporting AUC values for pTau217 starts with a 1.3× advantage over an Introduction chunk summarising background literature. The Introduction chunk may contain every query term; the Results chunk contains the answer.</p>

<p><strong>What this does not solve:</strong> Section detection is heuristic. A paper whose Results section is titled “Findings” or “Outcomes” will not be detected — those chunks land in <code class="language-plaintext highlighter-rouge">Body</code> at 1.0. For multi-disease corpora sourced from diverse journals, section header conventions vary. If section weights are not engaging, check chunk-level <code class="language-plaintext highlighter-rouge">section</code> distributions first.</p>

<h3 id="signal-2-term-density">Signal 2: Term Density</h3>

<p>Term density is the fraction of a chunk’s tokens that are query tokens:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">chunk_token_set</span> <span class="o">=</span> <span class="nb">set</span><span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="n">chunk</span><span class="p">.</span><span class="n">tokens</span><span class="p">)</span>
<span class="k">if</span> <span class="n">chunk_token_set</span><span class="p">:</span>
    <span class="n">density</span> <span class="o">=</span> <span class="nb">len</span><span class="p">(</span><span class="n">query_tokens</span> <span class="o">&amp;</span> <span class="n">chunk_token_set</span><span class="p">)</span> <span class="o">/</span> <span class="nb">len</span><span class="p">(</span><span class="n">chunk_token_set</span><span class="p">)</span>
</code></pre></div></div>

<p>A chunk where 15% of all tokens are query terms is denser and likely more focused than one where the same five query terms appear across 500 tokens of surrounding context. The density contributes a <code class="language-plaintext highlighter-rouge">(1 + density × 0.4)</code> multiplier, capping at a 40% bonus for a perfectly dense chunk.</p>

<p>This is a mild signal. Its main job is to break ties between two chunks from the same section of the same paper. A shorter, more focused chunk should win over a longer one that matches the same terms but buries them in noise.</p>

<h3 id="signal-3-discriminative-token-recall">Signal 3: Discriminative-Token Recall</h3>

<p>This is the signal that matters most and the one that requires the most explanation.</p>

<h4 id="the-root-problem">The Root Problem</h4>

<p>BM25 scores a chunk based on the terms it <em>contains</em>. It has no concept of terms a chunk is <em>missing</em>. This is fine for general-domain retrieval. In a multi-disease biomedical corpus, it is a structural problem.</p>

<p>Consider a query: <em>“What plasma biomarkers predict Alzheimer’s disease?”</em></p>

<p>After tokenisation and stopword removal, the query produces tokens including: <code class="language-plaintext highlighter-rouge">alzheimer</code>, <code class="language-plaintext highlighter-rouge">plasma</code>, <code class="language-plaintext highlighter-rouge">biomarker</code>, <code class="language-plaintext highlighter-rouge">biomarkers</code>, <code class="language-plaintext highlighter-rouge">predict</code>.</p>

<p>Now consider two chunks:</p>

<ul>
  <li><strong>Chunk A</strong> (from an Alzheimer’s paper): mentions <code class="language-plaintext highlighter-rouge">alzheimer</code>, <code class="language-plaintext highlighter-rouge">plasma</code>, <code class="language-plaintext highlighter-rouge">biomarker</code>, <code class="language-plaintext highlighter-rouge">amyloid</code>, <code class="language-plaintext highlighter-rouge">pTau217</code></li>
  <li><strong>Chunk B</strong> (from a lung cancer paper): mentions <code class="language-plaintext highlighter-rouge">plasma</code>, <code class="language-plaintext highlighter-rouge">biomarker</code>, <code class="language-plaintext highlighter-rouge">biomarkers</code>, <code class="language-plaintext highlighter-rouge">predict</code>, <code class="language-plaintext highlighter-rouge">detect</code>, <code class="language-plaintext highlighter-rouge">egfr</code>, <code class="language-plaintext highlighter-rouge">nsclc</code></li>
</ul>

<p>Chunk B matches four query tokens. It scores high. It has never heard of Alzheimer’s.</p>

<p>After ingesting Alzheimer’s papers alongside oncology papers in the BioRAG corpus, Chunk B-type results appeared consistently in the top 10. The false positive rate at rank 5 was unacceptable for a clinical decision-support context.</p>

<h4 id="why-predict-makes-it-worse">Why “Predict” Makes It Worse</h4>

<p>The naive fix is: check whether the query’s entity tokens (<code class="language-plaintext highlighter-rouge">alzheimer</code>) appear in the chunk. If not, penalise it.</p>

<p>This works, but it does not go far enough. In the example above, “predict” is in the query and in Chunk B. If you treat “predict” as a discriminative query token, then Chunk B partially escapes the penalty because it matched a query token — just not the disease-specific one.</p>

<p>Here is the insight that required debugging to discover: <strong>clinical verbs are not discriminative</strong>. “Predict”, “detect”, “identify”, “assess”, “evaluate” — these words appear in virtually every biomedical paper regardless of disease area. They carry no signal about <em>which</em> disease a paper covers. A lung cancer paper predicts things. A diabetes paper detects things. Using their presence in a chunk as evidence of topical relevance is simply noise.</p>

<p>The first version of the reranker treated all query tokens equally when checking entity recall. A lung cancer paper still landed at rank 5 for an Alzheimer’s query. It matched “predict”, “detect”, and “biomarker”, so its entity factor was 0.6 + 0.4 × (3/5) = 0.84. That was still too high.</p>

<h4 id="the-_generic_bio_terms-set">The <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code> Set</h4>

<p>The fix is to define a set of terms that appear in virtually every biomedical paper and exclude them from the discriminative-token calculation:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">_GENERIC_BIO_TERMS</span><span class="p">:</span> <span class="nb">frozenset</span><span class="p">[</span><span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="nb">frozenset</span><span class="p">({</span>
    <span class="c1"># Domain nouns ubiquitous across all disease areas
</span>    <span class="s">"biomarker"</span><span class="p">,</span> <span class="s">"biomarkers"</span><span class="p">,</span> <span class="s">"marker"</span><span class="p">,</span> <span class="s">"markers"</span><span class="p">,</span> <span class="s">"disease"</span><span class="p">,</span> <span class="s">"diseases"</span><span class="p">,</span>
    <span class="s">"patient"</span><span class="p">,</span> <span class="s">"patients"</span><span class="p">,</span> <span class="s">"study"</span><span class="p">,</span> <span class="s">"studies"</span><span class="p">,</span> <span class="s">"treatment"</span><span class="p">,</span> <span class="s">"treatments"</span><span class="p">,</span>
    <span class="s">"expression"</span><span class="p">,</span> <span class="s">"level"</span><span class="p">,</span> <span class="s">"levels"</span><span class="p">,</span> <span class="s">"plasma"</span><span class="p">,</span> <span class="s">"blood"</span><span class="p">,</span> <span class="s">"serum"</span><span class="p">,</span>
    <span class="s">"cell"</span><span class="p">,</span> <span class="s">"cells"</span><span class="p">,</span> <span class="s">"protein"</span><span class="p">,</span> <span class="s">"proteins"</span><span class="p">,</span> <span class="s">"gene"</span><span class="p">,</span> <span class="s">"genes"</span><span class="p">,</span>
    <span class="p">...</span>
    <span class="c1"># Common clinical/scientific verbs used in queries about any disease
</span>    <span class="s">"predict"</span><span class="p">,</span> <span class="s">"predicted"</span><span class="p">,</span> <span class="s">"predictive"</span><span class="p">,</span> <span class="s">"predictor"</span><span class="p">,</span> <span class="s">"predictors"</span><span class="p">,</span>
    <span class="s">"detect"</span><span class="p">,</span> <span class="s">"detected"</span><span class="p">,</span> <span class="s">"detectable"</span><span class="p">,</span> <span class="s">"identify"</span><span class="p">,</span> <span class="s">"identified"</span><span class="p">,</span>
    <span class="s">"assess"</span><span class="p">,</span> <span class="s">"assessed"</span><span class="p">,</span> <span class="s">"assessment"</span><span class="p">,</span> <span class="s">"evaluate"</span><span class="p">,</span> <span class="s">"evaluated"</span><span class="p">,</span>
    <span class="p">...</span>
    <span class="c1"># Common adjectives appearing in clinical queries across all diseases
</span>    <span class="s">"early"</span><span class="p">,</span> <span class="s">"late"</span><span class="p">,</span> <span class="s">"novel"</span><span class="p">,</span> <span class="s">"potential"</span><span class="p">,</span> <span class="s">"effective"</span><span class="p">,</span> <span class="s">"accurate"</span><span class="p">,</span>
    <span class="s">"sensitive"</span><span class="p">,</span> <span class="s">"specific"</span><span class="p">,</span> <span class="s">"elevated"</span><span class="p">,</span> <span class="s">"increased"</span><span class="p">,</span> <span class="s">"decreased"</span><span class="p">,</span>
    <span class="p">...</span>
<span class="p">})</span>
</code></pre></div></div>

<p>This is approximately 80 terms — a deliberately conservative list. “Biomarker” belongs to generic terms. “Alzheimer” does not.</p>

<h4 id="the-discriminative-token-recall-penalty">The Discriminative-Token Recall Penalty</h4>

<p>With this set defined, the discriminative tokens for a query are:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">discriminative</span> <span class="o">=</span> <span class="n">query_tokens</span> <span class="o">-</span> <span class="bp">self</span><span class="p">.</span><span class="n">_GENERIC_BIO_TERMS</span>
</code></pre></div></div>

<p>For the query <em>“What plasma biomarkers predict Alzheimer’s disease?”</em>, the discriminative tokens are <code class="language-plaintext highlighter-rouge">{"alzheimer"}</code>. “Plasma”, “biomarker”, “predict”, and “disease” are all in <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code> and get excluded.</p>

<p>The penalty is then applied based on how many discriminative tokens a chunk matches:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">discriminative</span><span class="p">:</span>
    <span class="n">matched</span> <span class="o">=</span> <span class="n">discriminative</span> <span class="o">&amp;</span> <span class="n">chunk_token_set</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">matched</span><span class="p">:</span>
        <span class="n">entity_factor</span> <span class="o">=</span> <span class="mf">0.15</span>   <span class="c1"># 85% penalty — demote hard
</span>    <span class="k">else</span><span class="p">:</span>
        <span class="n">entity_factor</span> <span class="o">=</span> <span class="mf">0.6</span> <span class="o">+</span> <span class="mf">0.4</span> <span class="o">*</span> <span class="p">(</span><span class="nb">len</span><span class="p">(</span><span class="n">matched</span><span class="p">)</span> <span class="o">/</span> <span class="nb">len</span><span class="p">(</span><span class="n">discriminative</span><span class="p">))</span>
<span class="k">else</span><span class="p">:</span>
    <span class="n">entity_factor</span> <span class="o">=</span> <span class="mf">1.0</span>  <span class="c1"># fully generic query — no penalty
</span></code></pre></div></div>

<p>Chunk B (the lung cancer paper) matches zero discriminative tokens. Its <code class="language-plaintext highlighter-rouge">entity_factor</code> is 0.15. Its adjusted score drops to roughly 15% of what BM25 gave it. It falls out of the top 5.</p>

<p>Chunk A (the Alzheimer’s paper) matches <code class="language-plaintext highlighter-rouge">alzheimer</code>. <code class="language-plaintext highlighter-rouge">entity_factor = 0.6 + 0.4 × (1/1) = 1.0</code>. Full score.</p>

<p><strong>Why 0.15 and not 0.0?</strong> A hard zero would permanently exclude any chunk that misses the discriminative tokens, even if the reranker’s discriminative set was narrowly defined. 0.15 keeps the chunk reachable in edge cases — for instance, a highly general query where all query tokens end up in <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code>, causing <code class="language-plaintext highlighter-rouge">discriminative</code> to be empty and triggering the <code class="language-plaintext highlighter-rouge">entity_factor = 1.0</code> fallback.</p>

<h4 id="the-full-scoring-formula">The Full Scoring Formula</h4>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">adjusted_score</span> <span class="o">=</span> <span class="p">(</span>
    <span class="n">r</span><span class="p">.</span><span class="n">score</span>            <span class="c1"># BM25 base score
</span>    <span class="o">*</span> <span class="n">section_w</span>        <span class="c1"># section weight (1.0 – 1.4×)
</span>    <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">density</span> <span class="o">*</span> <span class="mf">0.4</span><span class="p">)</span>  <span class="c1"># term density bonus (1.0 – 1.4×)
</span>    <span class="o">*</span> <span class="n">position_bonus</span>   <span class="c1"># early-rank bonus (decays as 1 + 0.05/rank)
</span>    <span class="o">*</span> <span class="n">entity_factor</span>    <span class="c1"># discriminative-token recall (0.15 – 1.0)
</span><span class="p">)</span>
</code></pre></div></div>

<p>The multiplicative structure ensures that a chunk must earn its place on multiple dimensions simultaneously. A Results chunk from a directly relevant paper outscores an Abstract chunk from a marginally relevant one, which outscores a Methods chunk from an off-topic paper — even if the off-topic paper had a higher raw BM25 score.</p>

<hr />

<h2 id="the-two-stage-debugging-process">The Two-Stage Debugging Process</h2>

<p>The reranker described above was not the first version. Here is how it was arrived at.</p>

<p><strong>Stage 1:</strong> After ingesting Alzheimer’s and oncology papers into the same index, the system was tested with the query <em>“What plasma biomarkers predict Alzheimer’s disease?”</em>. Rank 5 was occupied by an EGFR-mutant NSCLC paper. The paper had never mentioned Alzheimer’s. BM25 score: 7.30. Relevance as displayed to the user after normalisation: 75%.</p>

<p>The first fix was to add a check for query entity tokens (capitalised terms extracted by the QueryAnalyzer). If a chunk did not contain “Alzheimer”, penalise it. Entity factor for the NSCLC paper: 0.15. Problem solved for this query.</p>

<p><strong>Stage 2:</strong> Testing a broader query: <em>“What biomarkers predict early-stage cancer?”</em>. No specific disease entity in the query. The discriminative set was <code class="language-plaintext highlighter-rouge">{"early-stage", "cancer"}</code>. A colorectal cancer paper matched “cancer” but not “early-stage”. <code class="language-plaintext highlighter-rouge">entity_factor = 0.6 + 0.4 × (1/2) = 0.8</code>. Reasonable.</p>

<p>But a different failure mode appeared: a diabetes paper that used “predict” and “biomarker” heavily was still appearing at rank 4 for the Alzheimer’s query, even after Stage 1 fixes. The discriminative set was <code class="language-plaintext highlighter-rouge">{"alzheimer"}</code>. The diabetes paper did not contain “alzheimer”. So why was its entity_factor not 0.15?</p>

<p>Investigation revealed the bug: “predict” was being added to <code class="language-plaintext highlighter-rouge">discriminative</code> because it was not yet in <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code>. The diabetes paper matched “predict” as a discriminative token, so <code class="language-plaintext highlighter-rouge">entity_factor = 0.6 + 0.4 × (1/2) = 0.8</code>. Still high enough to survive.</p>

<p>The fix: add “predict”, “detect”, “identify”, “assess”, and all their morphological variants to <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code>. After this addition, the diabetes paper’s discriminative match dropped to zero. <code class="language-plaintext highlighter-rouge">entity_factor = 0.15</code>. It left the top 5.</p>

<p><strong>The lesson:</strong> When an off-topic paper survives an entity penalty at higher-than-expected strength, check whether it matched a query token that <em>should</em> have been classified as generic. The diagnostic question is: <em>would a paper about a completely different disease also contain this token?</em> If yes, it belongs in <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code>.</p>

<hr />

<h2 id="stage-3-evidence-classification">Stage 3: Evidence Classification</h2>

<p>After reranking, the pipeline has 5 chunks it believes are relevant. The <code class="language-plaintext highlighter-rouge">EvidenceClassifier</code> now asks a different question: <em>in what way is each chunk relevant?</em></p>

<p>Three categories:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">direct</code></strong> — the chunk contains statistical results, significance statements, or explicit confirmations that directly address the query</li>
  <li><strong><code class="language-plaintext highlighter-rouge">indirect</code></strong> — the chunk is topically related but does not directly answer the question</li>
  <li><strong><code class="language-plaintext highlighter-rouge">contradictory</code></strong> — the chunk contains language suggesting refutation, failure, or conflicting findings</li>
</ul>

<h3 id="why-a-three-way-split-matters-more-than-a-continuous-score">Why a Three-Way Split Matters More Than a Continuous Score</h3>

<p>A relevance score tells you <em>how much</em> a chunk matched the query. Evidence type tells you <em>how it matched</em>.</p>

<p>Consider two chunks for the query “Does pTau217 predict Alzheimer’s disease?”:</p>

<ul>
  <li>Chunk A: <em>“pTau217 demonstrated an AUC of 0.94 (95% CI: 0.91–0.97) for discriminating Alzheimer’s disease from controls, outperforming all other tested plasma biomarkers.”</em> — <strong>direct</strong></li>
  <li>Chunk B: <em>“While pTau217 showed promise in controlled cohorts, results failed to replicate in community-based samples with mixed pathologies.”</em> — <strong>contradictory</strong></li>
</ul>

<p>Both chunks are highly relevant. Both match the query entities. But they point in opposite directions. A system that returns only a relevance score conflates them. A clinician who receives only Chunk A has an incomplete picture. The three-way classification surfaces this explicitly in the <code class="language-plaintext highlighter-rouge">DecisionOutput</code> and flags it as a knowledge gap.</p>

<h3 id="how-classification-works">How Classification Works</h3>

<p>The <code class="language-plaintext highlighter-rouge">EvidenceClassifier</code> applies two ordered pattern lists to the chunk text:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">DIRECT_PATTERNS</span> <span class="o">=</span> <span class="p">[</span>
    <span class="sa">r</span><span class="s">'\b(demonstrate|show|reveal|confirm|establish|prove|indicate|suggest)\b'</span><span class="p">,</span>
    <span class="sa">r</span><span class="s">'\b(significant|p\s*[&lt;=&gt;]\s*0\.\d+|95%\s*CI|odds ratio|hazard ratio)\b'</span><span class="p">,</span>
    <span class="sa">r</span><span class="s">'\b(result|finding|observation|data|evidence)\b.*\b(support|consistent)\b'</span><span class="p">,</span>
<span class="p">]</span>

<span class="n">CONTRADICTORY_PATTERNS</span> <span class="o">=</span> <span class="p">[</span>
    <span class="sa">r</span><span class="s">'\b(contradict|inconsistent|conflict|contrary|dispute|challenge|refute)\b'</span><span class="p">,</span>
    <span class="sa">r</span><span class="s">'\b(however|nevertheless|in contrast|on the other hand)\b.*\b(no|not|fail)\b'</span><span class="p">,</span>
    <span class="sa">r</span><span class="s">'\b(no significant|non-significant|failed to|did not|could not)\b'</span><span class="p">,</span>
<span class="p">]</span>
</code></pre></div></div>

<p>Contradictory is checked first. A chunk that matches a contradictory pattern is classified as <code class="language-plaintext highlighter-rouge">contradictory</code> regardless of whether it also matches a direct pattern. A chunk can simultaneously describe a positive finding and then qualify or refute it. Labelling it <code class="language-plaintext highlighter-rouge">direct</code> when it contains an explicit contradiction would bury the caveat.</p>

<p>If no contradictory pattern matches, direct patterns are tested. A match on <code class="language-plaintext highlighter-rouge">p &lt; 0.001</code> or <code class="language-plaintext highlighter-rouge">hazard ratio</code> is nearly always a quantitative result and the chunk is classified <code class="language-plaintext highlighter-rouge">direct</code>. If neither pattern family matches, the chunk defaults to <code class="language-plaintext highlighter-rouge">indirect</code>.</p>

<p><strong>What the patterns look for:</strong></p>

<p>The direct patterns are structured around three evidence signatures:</p>

<ol>
  <li><strong>Reporting verbs</strong>: “demonstrate”, “reveal”, “confirm”. These are the verbs authors use when stating a result, as opposed to speculative verbs (“suggest”, “propose”) used in Discussion sections.</li>
  <li><strong>Statistical notation</strong>: <code class="language-plaintext highlighter-rouge">p &lt; 0.05</code>, <code class="language-plaintext highlighter-rouge">95% CI</code>, <code class="language-plaintext highlighter-rouge">odds ratio</code>. The presence of any statistical reporting is strong evidence the chunk contains a primary finding.</li>
  <li><strong>Consistency statements</strong>: “evidence consistent with”, “findings support”. These appear in meta-analyses and systematic reviews when summarising multi-study conclusions.</li>
</ol>

<p>The contradictory patterns target the linguistic markers of scientific refutation: adversative conjunctions followed by negation (“however … no”), failure statements (“failed to replicate”, “did not reach significance”), and explicit conflict language.</p>

<h3 id="key-terms-extraction">Key Terms Extraction</h3>

<p>Alongside the support type, the classifier extracts <code class="language-plaintext highlighter-rouge">key_terms</code> — the terms that justify the classification and will surface in the final <code class="language-plaintext highlighter-rouge">DecisionOutput</code>:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">_extract_key_terms</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">query_analysis</span><span class="p">:</span> <span class="nb">dict</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
    <span class="n">entities</span> <span class="o">=</span> <span class="n">re</span><span class="p">.</span><span class="n">findall</span><span class="p">(</span>
        <span class="sa">r</span><span class="s">'\b[A-Z][A-Za-z0-9\-]{2,}(?:\s+[A-Z][A-Za-z0-9\-]{2,}){0,2}\b'</span><span class="p">,</span> <span class="n">text</span>
    <span class="p">)</span>
    <span class="n">stats</span> <span class="o">=</span> <span class="n">re</span><span class="p">.</span><span class="n">findall</span><span class="p">(</span>
        <span class="sa">r</span><span class="s">'(?:p\s*[&lt;=&gt;]\s*0\.\d+|OR\s*=?\s*[\d.]+|HR\s*=?\s*[\d.]+|'</span>
        <span class="sa">r</span><span class="s">'95%\s*CI|RR\s*=?\s*[\d.]+|\d+\.?\d*%)'</span><span class="p">,</span> <span class="n">text</span>
    <span class="p">)</span>
    <span class="n">query_terms</span> <span class="o">=</span> <span class="p">[</span><span class="n">t</span> <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">query_analysis</span><span class="p">[</span><span class="s">"tokens"</span><span class="p">]</span> <span class="k">if</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">text</span><span class="p">.</span><span class="n">lower</span><span class="p">()</span> <span class="ow">and</span> <span class="nb">len</span><span class="p">(</span><span class="n">t</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">3</span><span class="p">]</span>
    <span class="n">combined</span> <span class="o">=</span> <span class="nb">list</span><span class="p">(</span><span class="nb">dict</span><span class="p">.</span><span class="n">fromkeys</span><span class="p">(</span><span class="n">entities</span><span class="p">[:</span><span class="mi">4</span><span class="p">]</span> <span class="o">+</span> <span class="n">stats</span><span class="p">[:</span><span class="mi">2</span><span class="p">]</span> <span class="o">+</span> <span class="n">query_terms</span><span class="p">[:</span><span class="mi">3</span><span class="p">]))</span>
    <span class="k">return</span> <span class="n">combined</span><span class="p">[:</span><span class="mi">6</span><span class="p">]</span>
</code></pre></div></div>

<p>Three extraction passes:</p>
<ul>
  <li><strong>Named entities</strong> (capitalised multi-word terms): gene names, drug names, disease names</li>
  <li><strong>Statistical values</strong>: AUC percentages, p-values, OR/HR notation</li>
  <li><strong>Query-matching terms</strong>: tokens from the original query that appear in the chunk, as confirmation of topical match</li>
</ul>

<p>The result is a list of up to 6 terms that link the chunk’s classification to the source text. This gives the synthesiser and the end user concrete anchors when reading the evidence.</p>

<h3 id="limitations-and-how-to-improve">Limitations and How to Improve</h3>

<p><strong>The patterns are surface-level.</strong> A chunk that uses “suggest” is labelled <code class="language-plaintext highlighter-rouge">direct</code> because “suggest” appears in <code class="language-plaintext highlighter-rouge">DIRECT_PATTERNS</code>. But authors use “suggest” both to report their own results (“our data suggest X”) and to speculate about mechanisms (“this may suggest X”). The former is direct evidence; the latter is not. Distinguishing them requires parsing sentence structure, not just matching a verb.</p>

<p><strong>Hedging is not captured.</strong> “pTau217 may be a useful biomarker” and “pTau217 is a validated biomarker” both match direct patterns. The modal “may” qualifies the strength of the claim substantially. A classifier that cannot detect epistemic hedging will overstate the strength of indirect evidence.</p>

<p><strong>Contradiction requires co-occurrence in one chunk.</strong> A paper can contradict another paper without any single chunk containing contradictory language. The current <code class="language-plaintext highlighter-rouge">EvidenceClassifier</code> operates at the chunk level; cross-chunk contradiction detection is a job for the <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> (covered in Post 3).</p>

<p><strong>How to improve it:</strong> A lightweight BERT-based fact verification model — NLI: does this chunk <em>entail</em>, <em>contradict</em>, or remain <em>neutral</em> relative to the query — would replace all three pattern families with a single inference pass. Models like <code class="language-plaintext highlighter-rouge">MedNLI</code> or <code class="language-plaintext highlighter-rouge">BioASQ-entailment</code> are trained exactly for this task on biomedical text. The pattern approach is a zero-dependency baseline that is interpretable and auditable; the NLI approach is more accurate but adds a model dependency.</p>

<hr />

<h2 id="what-the-pipeline-produces-after-stage-3">What the Pipeline Produces After Stage 3</h2>

<p>After <code class="language-plaintext highlighter-rouge">Reranker</code> and <code class="language-plaintext highlighter-rouge">EvidenceClassifier</code>, the pipeline has transformed 15 raw BM25 candidates into 5 structured <code class="language-plaintext highlighter-rouge">EvidenceNode</code> objects:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">evidence_nodes</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">EvidenceNode</span><span class="p">(</span>
        <span class="n">doc_title</span><span class="o">=</span><span class="s">"Plasma Aβ42/p-Tau217 ratio as a blood-based biomarker..."</span><span class="p">,</span>
        <span class="n">section</span><span class="o">=</span><span class="s">"Results"</span><span class="p">,</span>
        <span class="n">excerpt</span><span class="o">=</span><span class="s">"Plasma pTau217 demonstrated an AUC of 0.94 (95% CI 0.91–0.97)..."</span><span class="p">,</span>
        <span class="n">relevance_score</span><span class="o">=</span><span class="mf">1.000</span><span class="p">,</span>   <span class="c1"># normalized to top result
</span>        <span class="n">support_type</span><span class="o">=</span><span class="s">"direct"</span><span class="p">,</span>
        <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"pTau217"</span><span class="p">,</span> <span class="s">"AUC"</span><span class="p">,</span> <span class="s">"0.94"</span><span class="p">,</span> <span class="s">"95% CI"</span><span class="p">,</span> <span class="s">"Alzheimer"</span><span class="p">],</span>
    <span class="p">),</span>
    <span class="n">EvidenceNode</span><span class="p">(</span>
        <span class="n">doc_title</span><span class="o">=</span><span class="s">"Diagnostic Blood-Based Biomarkers for Alzheimer's Disease"</span><span class="p">,</span>
        <span class="n">section</span><span class="o">=</span><span class="s">"Results"</span><span class="p">,</span>
        <span class="n">excerpt</span><span class="o">=</span><span class="s">"Amyloid-β42/40 ratio and NfL showed significant associations..."</span><span class="p">,</span>
        <span class="n">relevance_score</span><span class="o">=</span><span class="mf">0.832</span><span class="p">,</span>
        <span class="n">support_type</span><span class="o">=</span><span class="s">"direct"</span><span class="p">,</span>
        <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"Amyloid"</span><span class="p">,</span> <span class="s">"NfL"</span><span class="p">,</span> <span class="s">"significant"</span><span class="p">,</span> <span class="s">"Alzheimer"</span><span class="p">],</span>
    <span class="p">),</span>
    <span class="n">EvidenceNode</span><span class="p">(</span>
        <span class="n">doc_title</span><span class="o">=</span><span class="s">"Longitudinal Changes in Plasma Biomarkers..."</span><span class="p">,</span>
        <span class="n">section</span><span class="o">=</span><span class="s">"Discussion"</span><span class="p">,</span>
        <span class="n">excerpt</span><span class="o">=</span><span class="s">"However, plasma biomarker sensitivity failed to reach clinical..."</span><span class="p">,</span>
        <span class="n">relevance_score</span><span class="o">=</span><span class="mf">0.641</span><span class="p">,</span>
        <span class="n">support_type</span><span class="o">=</span><span class="s">"contradictory"</span><span class="p">,</span>
        <span class="n">key_terms</span><span class="o">=</span><span class="p">[</span><span class="s">"plasma"</span><span class="p">,</span> <span class="s">"sensitivity"</span><span class="p">,</span> <span class="s">"failed"</span><span class="p">,</span> <span class="s">"clinical threshold"</span><span class="p">],</span>
    <span class="p">),</span>
    <span class="c1"># ...
</span><span class="p">]</span>
</code></pre></div></div>

<p>The lung cancer paper is gone. The Alzheimer’s papers are ranked by a combination of BM25 score, section relevance to the diagnosis intent, and entity specificity. The contradictory evidence is flagged explicitly rather than buried.</p>

<p>These nodes are what the <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> and <code class="language-plaintext highlighter-rouge">AnswerSynthesizer</code> receive as input.</p>

<hr />

<h2 id="where-semantic-search-would-help">Where Semantic Search Would Help</h2>

<p>Two parts of the reranker are still rule-based in ways that limit them.</p>

<p>Section detection (<code class="language-plaintext highlighter-rouge">_detect_section</code>) matches literal header strings, so a chunk labelled “Key Findings” or “Clinical Outcomes” falls back to <code class="language-plaintext highlighter-rouge">Body</code> at weight 1.0 and never receives the Results boost. Embedding the header and comparing it to canonical section descriptions would fix this with no changes to the downstream scoring.</p>

<p>The <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code> frozenset has the same structural weakness: it is hand-curated, misses morphological variants (“predictability” is not in the list even though “predict” is), and cannot adapt to a corpus that is heavy in one sub-domain where additional terms become universally generic. Replacing it with a corpus-driven genericity score — computed by clustering documents into disease-area groups and measuring how uniformly each vocabulary term appears across clusters — would make the discriminative-token boundary data-derived and self-updating as the corpus grows.</p>

<hr />

<h2 id="coming-up-in-post-3">Coming Up in Post 3</h2>

<p>Post 3 covers the <code class="language-plaintext highlighter-rouge">KnowledgeGapDetector</code> and <code class="language-plaintext highlighter-rouge">AnswerSynthesizer</code>. We will look at how the system decides when it does <em>not</em> know the answer and how it constructs an explicit, step-by-step reasoning chain that makes its uncertainty auditable.</p>

<p>The confidence scoring formula derives a 0–1 score from four inputs: evidence count, direct-to-indirect ratio, gap count, and contradiction presence. We will trace why this produces calibrated confidence for sparse evidence and why “calibrated” matters more than “high” in a clinical context.</p>

<p>We will also look at the <code class="language-plaintext highlighter-rouge">FollowUpGenerator</code> and how intent-aware follow-up questions can drive a user from an initial query toward a more precisely answerable one.</p>

<hr />

<p><em>BioRAG is built in pure Python stdlib. The core engine — BM25 index, reranker, evidence classifier, synthesizer — has zero external dependencies. PubMed ingestion requires only <code class="language-plaintext highlighter-rouge">requests</code>. LLM answer synthesis requires only <code class="language-plaintext highlighter-rouge">anthropic</code>. Github: https://github.com/ricchasethi/rag-biomedical-decision</em></p>]]></content><author><name></name></author><summary type="html"><![CDATA[BioRAG]]></summary></entry><entry><title type="html">BioRAG: Part 1</title><link href="/2026/05/11/BioRAG-part1-introduction-query-retrieval.html" rel="alternate" type="text/html" title="BioRAG: Part 1" /><published>2026-05-11T00:00:00+10:00</published><updated>2026-05-11T00:00:00+10:00</updated><id>/2026/05/11/BioRAG-part1-introduction-query-retrieval</id><content type="html" xml:base="/2026/05/11/BioRAG-part1-introduction-query-retrieval.html"><![CDATA[<h1 id="biorag">BioRAG</h1>

<p><strong>Part 1 of 5 — Introduction, Query Understanding, and Retrieval</strong></p>

<hr />

<h2 id="why-biomedical-literature-needs-a-different-approach">Why Biomedical Literature Needs a Different Approach</h2>

<p>The biomedical literature problem is unlike any other information retrieval problem. PubMed indexes over 35 million citations. A working clinician or researcher asking “which plasma biomarkers best predict early Alzheimer’s disease?” is not looking for the ten most popular documents about Alzheimer’s or biomarkers. They are looking for specific, evidenced, traceable answers that can inform a real decision.</p>

<p>General-purpose RAG systems fall short here for three reasons.</p>

<p><strong>Precision matters more than recall.</strong> A chatbot that confidently synthesises an answer from tangentially relevant papers is worse than useless in a clinical context. An answer needs to be traceable to the specific passage that supports it, with an explicit statement of how strong that support is.</p>

<p><strong>The vocabulary is highly specialised.</strong> Biomedical text is dense with abbreviations (PD-L1, APOE4, pTau217), statistical notation (AUC, HR, CI), and entity names that carry the entire discriminative signal for a query. Semantic embeddings trained on general corpora can miss the distinction between “AD biomarkers” and “cardiovascular biomarkers” because both look superficially similar in vector space.</p>

<p><strong>Uncertainty must be surfaced, not buried.</strong> When the corpus does not contain enough evidence to answer a question confidently, the system must say so. Calibrated uncertainty is a feature, not a limitation.</p>

<p>BioRAG was built to address all three. It is a retrieval-augmented generation engine that returns not just an answer, but a structured decision output: an evidence classification, an explicit reasoning chain, a confidence score, and a list of knowledge gaps.</p>

<hr />

<h2 id="the-series-roadmap">The Series Roadmap</h2>

<p>This is the first of five posts. Here is where we are going:</p>

<table>
  <thead>
    <tr>
      <th>Post</th>
      <th>Topic</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>1 (this post)</strong></td>
      <td>Introduction · Query Analyzer · BM25 Retrieval</td>
    </tr>
    <tr>
      <td>2</td>
      <td>Reranking · Evidence Classification · False Positive Suppression</td>
    </tr>
    <tr>
      <td>3</td>
      <td>Knowledge Gap Detection · Confidence Scoring · Reasoning Chains</td>
    </tr>
    <tr>
      <td>4</td>
      <td>LLM Answer Synthesis · Grounding · Prompt Design</td>
    </tr>
    <tr>
      <td>5</td>
      <td>Evaluation · MRR · NDCG · Lessons from Building Retrieval Evals</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="the-pipeline-at-a-glance">The Pipeline at a Glance</h2>

<p>Before going deep, here is the full pipeline so you can orient each post within it:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Query
  │
  ▼
QueryAnalyzer          ← intent, entities, expanded tokens
  │
  ▼
InvertedIndex (BM25)   ← top-K candidate chunks            ← this post
  │
  ▼
Reranker               ← section-aware + entity penalty    ← Post 2
  │
  ▼
EvidenceClassifier     ← direct / indirect / contradictory ← Post 2
  │
  ▼
KnowledgeGapDetector   ← missing data, contradictions      ← Post 3
  │
  ▼
AnswerSynthesizer      ← reasoning chain + confidence      ← Posts 3 &amp; 4
  │
  ▼
DecisionOutput         ← structured, auditable answer
</code></pre></div></div>

<p>Each stage is a separate class with a single responsibility. The design is intentional: you can upgrade any one stage like swap BM25 for dense embeddings, or replace the rule-based synthesizer with an LLM.</p>

<hr />

<h2 id="stage-1-query-analyzer">Stage 1: Query Analyzer</h2>

<p>The <code class="language-plaintext highlighter-rouge">QueryAnalyzer</code> is the first stage in the pipeline, and its job is deceptively simple: turn a natural language question into a structured representation the rest of the pipeline can act on. In practice, getting this right is most of the work.</p>

<h3 id="intent-classification">Intent Classification</h3>

<p>Not all biomedical questions are the same. “What is the mechanism of PD-L1 inhibition?” asks for a biological explanation. “Which treatment is better: ceftazidime-avibactam or colistin for CRE?” asks for a comparison. “What is the 5-year survival rate for stage III NSCLC?” asks for a prognostic figure.</p>

<p>These different intents should retrieve from different parts of a paper and weight different evidence types differently. A mechanism query should prioritise Methods and Results sections. A treatment comparison should prioritise Results and Discussion. A definition query should prioritise the Introduction and Abstract.</p>

<p>BioRAG classifies queries into seven intent types — <code class="language-plaintext highlighter-rouge">mechanism</code>, <code class="language-plaintext highlighter-rouge">comparison</code>, <code class="language-plaintext highlighter-rouge">treatment</code>, <code class="language-plaintext highlighter-rouge">diagnosis</code>, <code class="language-plaintext highlighter-rouge">prognosis</code>, <code class="language-plaintext highlighter-rouge">epidemiology</code>, and <code class="language-plaintext highlighter-rouge">general</code>. It is done by matching trigger phrases against the query. This drives section weighting in the reranker downstream.</p>

<p><strong>What this looks like in practice:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Query:   "What biomarkers predict Alzheimer's disease?"
Intent:  diagnosis
Entities: ["Alzheimer"]

Query:   "Compare ceftazidime-avibactam versus colistin for CRE"
Intent:  comparison
Entities: ["CRE", "Ceftazidime-avibactam", "Colistin"]
</code></pre></div></div>

<p><strong>Key parameter: trigger phrase coverage.</strong> If a query does not match any trigger phrase it falls back to <code class="language-plaintext highlighter-rouge">general</code>, losing all the intent-specific section boosting downstream. Regularly audit misclassified queries from your eval set against the trigger phrase list.</p>

<p><strong>How to improve it:</strong> The trigger-phrase approach is brittle. Replacing it with a small fine-tuned classifier (even a logistic regression over TF-IDF features trained on a few hundred labelled queries) lifts accuracy significantly with minimal infrastructure cost.</p>

<h3 id="entity-extraction">Entity Extraction</h3>

<p>Biomedical queries are almost always anchored by one or two named entities i.e. a disease, a gene, a drug, a pathogen. These entities are the discriminative signal that separates “Alzheimer’s biomarkers” from “cardiovascular biomarkers” even when both queries contain the word “biomarker” many times over.</p>

<p>The <code class="language-plaintext highlighter-rouge">QueryAnalyzer</code> extracts entities by identifying capitalised multi-word terms, filtered against a blocklist of question words (<code class="language-plaintext highlighter-rouge">What</code>, <code class="language-plaintext highlighter-rouge">Which</code>, <code class="language-plaintext highlighter-rouge">How</code> etc). This is intentionally lightweight i.e. no NER model, no external API.</p>

<p><strong>Key limitation: case sensitivity.</strong> The entity extractor relies on capitalisation. A query typed entirely in lowercase — <code class="language-plaintext highlighter-rouge">alzheimer's disease biomarkers</code> — yields no entities. This matters because entity presence drives the false-positive suppression penalty in the reranker (covered in Post 2). We address this with a <code class="language-plaintext highlighter-rouge">_GENERIC_BIO_TERMS</code> approach rather than entity-only logic, but a case-insensitive NER model would be the cleaner long-term fix.</p>

<p><strong>How to improve it:</strong> Integrate a lightweight biomedical NER model such as <a href="https://huggingface.co/pruas/BENT-PubMedBERT-NER-Gene">PubMedBERT-NER</a> or the scispaCy <code class="language-plaintext highlighter-rouge">en_ner_bc5cdr_md</code> pipeline (covers diseases and chemicals). Even a dictionary lookup against the UMLS Metathesaurus would lift entity recall dramatically for common biomedical terms.</p>

<h3 id="abbreviation-expansion">Abbreviation Expansion</h3>

<p>Biomedical abbreviations are a retrieval hazard. A query containing “PD-L1” should also match documents that spell it out as “programmed death-ligand 1”. A query about “PCR” should match “polymerase chain reaction”. Without expansion, these queries silently miss relevant passages.</p>

<p>BioRAG maintains an <code class="language-plaintext highlighter-rouge">ABBREV_MAP</code> that expands common abbreviations at query time, appending the expanded tokens to the query token list. The original abbreviation is preserved so both short and long forms match.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># In QueryAnalyzer.analyze()
</span><span class="n">expanded_tokens</span> <span class="o">=</span> <span class="nb">list</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>
<span class="k">for</span> <span class="n">token</span> <span class="ow">in</span> <span class="n">tokens</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">token</span><span class="p">.</span><span class="n">lower</span><span class="p">()</span> <span class="ow">in</span> <span class="bp">self</span><span class="p">.</span><span class="n">processor</span><span class="p">.</span><span class="n">ABBREV_MAP</span><span class="p">:</span>
        <span class="n">expansion</span> <span class="o">=</span> <span class="bp">self</span><span class="p">.</span><span class="n">processor</span><span class="p">.</span><span class="n">tokenize</span><span class="p">(</span><span class="bp">self</span><span class="p">.</span><span class="n">processor</span><span class="p">.</span><span class="n">ABBREV_MAP</span><span class="p">[</span><span class="n">token</span><span class="p">.</span><span class="n">lower</span><span class="p">()])</span>
        <span class="n">expanded_tokens</span><span class="p">.</span><span class="n">extend</span><span class="p">(</span><span class="n">expansion</span><span class="p">)</span>
</code></pre></div></div>

<p><strong>How to improve it:</strong> The current map is hand-curated and covers around 15 common abbreviations. The right approach at scale is to extract abbreviation-definition pairs directly from the corpus using the Schwartz-Hearst algorithm — the pattern <code class="language-plaintext highlighter-rouge">term (ABBREV)</code> appears in nearly every methods section. This means the expansion map grows automatically with whatever literature you ingest, at zero maintenance cost.</p>

<hr />

<h2 id="stage-2-bm25-retrieval">Stage 2: BM25 Retrieval</h2>

<p>Once the query is analysed, the <code class="language-plaintext highlighter-rouge">InvertedIndex</code> retrieves the top-K candidate chunks using BM25, specifically the Okapi BM25 variant.</p>

<h3 id="why-bm25-and-not-dense-embeddings">Why BM25 and Not Dense Embeddings?</h3>

<p>This is the most common question about this system, so let me address it directly.</p>

<p>Dense embedding models (sentence-transformers, OpenAI text-embedding-3) encode semantic similarity. They are excellent at finding documents that <em>mean</em> the same thing even when they use different words. For general-domain retrieval this is a significant advantage.</p>

<p>For biomedical retrieval, it becomes a liability in two ways.</p>

<p>First, the vocabulary is so specialised that semantic proximity in embedding space can mislead. “APOE4 genotype” and “cardiovascular risk gene” might map to nearby vectors even though they describe entirely different biology. BM25 matches on exact tokens and exact tokens are the evidence in biomedicine.</p>

<p>Second, dense embeddings require either a GPU, an external API call, or a large local model. BM25 is pure arithmetic over a term-frequency index. For a system that emphasises zero external dependencies and auditability, this matters. BioRAG’s core engine runs with no installs beyond the Python standard library.</p>

<p>That said, BM25 and dense retrieval are <strong>complementary, not competing</strong>. The right production architecture is a hybrid: run both in parallel and merge scores with reciprocal rank fusion. We will return to this in Post 5.</p>

<h3 id="how-bm25-works">How BM25 Works</h3>

<p>BM25 scores a chunk against a query by summing the contribution of each matching query term, weighted by:</p>

<ul>
  <li><strong>IDF (inverse document frequency):</strong> rare terms score higher. “Alzheimer” scores far higher than “biomarker” because it appears in far fewer documents.</li>
  <li><strong>Term frequency saturation:</strong> each additional occurrence of a term adds diminishing returns, controlled by the parameter <code class="language-plaintext highlighter-rouge">K1</code>. At <code class="language-plaintext highlighter-rouge">K1 = 1.5</code>, a term appearing 10 times in a chunk does not score 10× a term appearing once.</li>
  <li><strong>Document length normalisation:</strong> longer chunks are penalised to prevent them from dominating simply by having more text. The parameter <code class="language-plaintext highlighter-rouge">B</code> controls this. At <code class="language-plaintext highlighter-rouge">B = 0.75</code>, a chunk twice the average length is moderately penalised.</li>
</ul>

<p>The scoring formula for a single term <code class="language-plaintext highlighter-rouge">t</code> in chunk <code class="language-plaintext highlighter-rouge">d</code> is:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>score(t, d) = IDF(t) × (TF(t,d) × (K1 + 1)) / (TF(t,d) + K1 × (1 - B + B × |d| / avgdl))
</code></pre></div></div>

<p>where <code class="language-plaintext highlighter-rouge">|d|</code> is the chunk length and <code class="language-plaintext highlighter-rouge">avgdl</code> is the average chunk length across the corpus.</p>

<h3 id="key-parameters">Key Parameters</h3>

<table>
  <thead>
    <tr>
      <th>Parameter</th>
      <th>Default</th>
      <th>Effect</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">K1</code></td>
      <td>1.5</td>
      <td>Controls term frequency saturation. Higher values reward repeated terms more. For biomedical text with high repetition of key terms, 1.2–1.5 is typical.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">B</code></td>
      <td>0.75</td>
      <td>Controls length normalisation. Higher = shorter chunks are favoured. For chunked text, 0.6–0.8 is reasonable.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">retrieval_top_k</code></td>
      <td>15</td>
      <td>Number of candidate chunks passed to the reranker. Higher improves recall but increases reranking cost.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chunk_size</code></td>
      <td>512 tokens</td>
      <td>Larger chunks retain more context but dilute term density. Smaller chunks are more precise but may split key sentences.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chunk_overlap</code></td>
      <td>64 tokens</td>
      <td>Overlap between adjacent chunks. Prevents key sentences from being split across boundaries.</td>
    </tr>
  </tbody>
</table>

<p><strong>Tuning <code class="language-plaintext highlighter-rouge">K1</code> and <code class="language-plaintext highlighter-rouge">B</code>:</strong> The defaults work well but are not optimised for any specific corpus. Run your retrieval eval harness (covered in Post 5) with a grid search over <code class="language-plaintext highlighter-rouge">K1 ∈ {1.0, 1.2, 1.5, 2.0}</code> and <code class="language-plaintext highlighter-rouge">B ∈ {0.5, 0.65, 0.75, 0.9}</code>. For a corpus dominated by short PubMed abstracts, a lower <code class="language-plaintext highlighter-rouge">B</code> (0.5) tends to perform better because length variation between chunks is small. For full-text PMC articles with long methods sections, a higher <code class="language-plaintext highlighter-rouge">B</code> (0.75–0.9) normalises the length advantage those sections get.</p>

<p><strong>Tuning <code class="language-plaintext highlighter-rouge">chunk_size</code>:</strong> Smaller chunks (256 tokens) improve precision. The retrieved passage is tightly about one thing, but hurt recall when the key sentence is surrounded by context. Larger chunks (1024 tokens) improve recall but return more noise per hit. 512 with 64-token overlap is a reasonable default for mixed abstract/full-text corpora.</p>

<h3 id="the-false-positive-problem">The False Positive Problem</h3>

<p>BM25’s fundamental weakness for multi-disease corpora is that it rewards term presence but has no concept of term absence. A query for “Alzheimer’s disease biomarkers” tokenises to <code class="language-plaintext highlighter-rouge">[alzheimer, disease, biomarker]</code>. A lung cancer paper that uses “biomarker” and “disease” repeatedly will score near the top of results — even though “alzheimer” never appears in it.</p>

<p>In a general-domain system, this is tolerable. In a decision-support system where a clinician is relying on the returned evidence nodes, surfacing an irrelevant paper at 75% relevance is a serious quality problem. We observed exactly this after ingesting Alzheimer’s papers alongside oncology papers.</p>

<p>The root cause is at the retrieval stage. The fix, however, lives in the reranker which has access to both the query’s entity tokens and the chunk content, and can apply a structured penalty. We will cover this in full in Post 2.</p>

<h3 id="how-to-improve-retrieval">How to Improve Retrieval</h3>

<p><strong>Short term:</strong></p>
<ul>
  <li>Tune <code class="language-plaintext highlighter-rouge">K1</code> and <code class="language-plaintext highlighter-rouge">B</code> on your eval set. A 1–2% NDCG gain is achievable with no code changes.</li>
  <li>Expand the <code class="language-plaintext highlighter-rouge">ABBREV_MAP</code> with domain-specific abbreviations from your target corpus. Every abbreviation you miss is a silent recall failure.</li>
  <li>Implement prefix matching carefully. BioRAG includes a prefix fallback for query terms with no exact match, but short-prefix matching (4 chars) produces false positives — <code class="language-plaintext highlighter-rouge">hemo</code> matches <code class="language-plaintext highlighter-rouge">heart</code>. Restrict prefix fallback to terms longer than 6 characters.</li>
</ul>

<p><strong>Medium term:</strong></p>
<ul>
  <li><strong>Query expansion via pseudo-relevance feedback (PRF):</strong> fetch the top-3 BM25 results, extract their most discriminative terms by TF-IDF, and add those to the query for a second retrieval pass. This is surprisingly effective for biomedical queries where the user may not know the exact terminology used in the target literature.</li>
  <li><strong>Synonym expansion via UMLS:</strong> map query entities to their UMLS concept IDs and expand with all synonyms. “Heart attack” and “myocardial infarction” become the same query token family.</li>
</ul>

<p><strong>Long term:</strong></p>
<ul>
  <li><strong>Hybrid BM25 + dense retrieval with RRF:</strong> run both retrievers in parallel, normalise their rank lists, and merge with reciprocal rank fusion <code class="language-plaintext highlighter-rouge">score = Σ 1/(k + rank_i)</code> where <code class="language-plaintext highlighter-rouge">k = 60</code> is standard. The two signals are complementary: BM25 handles rare exact terms; the encoder handles paraphrases.</li>
  <li><strong>Learned sparse representations (SPLADE):</strong> a middle ground between BM25 and dense retrieval. SPLADE learns to expand document and query representations into sparse vectors over the vocabulary, preserving the interpretability of BM25 while learning term weights from data.</li>
</ul>

<hr />

<h2 id="what-the-first-two-stages-produce">What the First Two Stages Produce</h2>

<p>After <code class="language-plaintext highlighter-rouge">QueryAnalyzer</code> and <code class="language-plaintext highlighter-rouge">InvertedIndex</code>, the pipeline has:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">query_analysis</span> <span class="o">=</span> <span class="p">{</span>
    <span class="s">"original"</span><span class="p">:</span> <span class="s">"What plasma biomarkers predict Alzheimer's disease?"</span><span class="p">,</span>
    <span class="s">"intent"</span><span class="p">:</span> <span class="s">"diagnosis"</span><span class="p">,</span>
    <span class="s">"entities"</span><span class="p">:</span> <span class="p">[</span><span class="s">"Alzheimer"</span><span class="p">],</span>
    <span class="s">"expanded_tokens"</span><span class="p">:</span> <span class="p">[</span><span class="s">"alzheimer"</span><span class="p">,</span> <span class="s">"plasma"</span><span class="p">,</span> <span class="s">"biomarker"</span><span class="p">,</span> <span class="s">"biomarkers"</span><span class="p">,</span> <span class="s">"predict"</span><span class="p">],</span>
<span class="p">}</span>

<span class="n">retrieved_chunks</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">RetrievedChunk</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Plasma Aβ42/p-Tau217 ratio..."</span><span class="p">,</span>       <span class="n">score</span><span class="o">=</span><span class="mf">9.72</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
    <span class="n">RetrievedChunk</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"Diagnostic Blood-Based Biomarkers..."</span><span class="p">,</span> <span class="n">score</span><span class="o">=</span><span class="mf">8.10</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span>
    <span class="n">RetrievedChunk</span><span class="p">(</span><span class="n">doc_title</span><span class="o">=</span><span class="s">"EGFR-Mutant Non-Small Cell Lung..."</span><span class="p">,</span>   <span class="n">score</span><span class="o">=</span><span class="mf">7.30</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">9</span><span class="p">),</span>
    <span class="c1"># ... up to retrieval_top_k chunks
</span><span class="p">]</span>
</code></pre></div></div>

<p>These 15 chunks are raw BM25 candidates. Some are excellent. Some like that lung cancer paper at rank 9 are off-topic papers that matched on generic terms. The next stage, the Reranker, is where precision is recovered.</p>

<hr />

<h2 id="coming-up-in-post-2">Coming Up in Post 2</h2>

<p>In the next post we go deep into the Reranker. We will look at how section weighting (prioritising Results over Introduction for factual queries) interacts with term density scoring. Additionally, we will see how the discriminative-token recall penalty was designed to suppress the false positive described above.</p>

<p>We will trace the two-stage debugging process that revealed why common clinical verbs like “predict” and “detect” must be treated as generic rather than discriminative, even when they appear in the query. A lung cancer paper showing up at 65% relevance for an Alzheimer’s query after the first fix is a useful lesson in how BM25’s term presence bias propagates into the reranker unless you’re precise about what “discriminative” means.</p>

<p>We will also cover the <code class="language-plaintext highlighter-rouge">EvidenceClassifier</code> — how a chunk is labelled as <code class="language-plaintext highlighter-rouge">direct</code>, <code class="language-plaintext highlighter-rouge">indirect</code>, or <code class="language-plaintext highlighter-rouge">contradictory</code>, and why this three-way distinction carries more signal for clinical decision support than a continuous relevance score alone.</p>

<hr />

<p><em>BioRAG is built in pure Python stdlib. The core engine — BM25 index, reranker, evidence classifier, synthesizer — has zero external dependencies. PubMed ingestion requires only <code class="language-plaintext highlighter-rouge">requests</code>. LLM answer synthesis requires only <code class="language-plaintext highlighter-rouge">anthropic</code>. Github: https://github.com/ricchasethi/rag-biomedical-decision</em></p>]]></content><author><name></name></author><summary type="html"><![CDATA[BioRAG]]></summary></entry></feed>